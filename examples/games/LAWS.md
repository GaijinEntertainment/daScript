# LAWS.md - Boris's rulings

Append-only intent provenance for this folder's rule documents (the mechanism: CLAUDE.md
sec. "Boris's rulings get a `LAWS.md` sidecar" at the repo root). Never groomed, compacted,
or cited as rules.

| Date | Document | The ask |
|---|---|---|
| 2026-08-28 | REVIEW.md (the staged-copy identity rule), REVIEW.das (check_port_identity) | Port convergence: "lets fix the ports. i'd like to get to the point where playground and web versions are identical" - after probing showed the wasm runtime resolves `live/*` natively ("its same thing. require ?live_host glfw_live or something like that" became unnecessary), the ports converged to byte-identical desktop files and the gate now bans the fork re-forming |
