# This example declares no external deps (dasGLTF / dasVulkan / dasImgui are all in-tree), but a
# stray `daspkg install` here still writes modules/ + daspkg.lock — build artifacts either way.
modules/
daspkg.lock
