options gen2

require daslib/daspkg

[export]
def package() {
    package_name("minfft-example")
    package_description("Example project using das-minfft via daspkg")
}

[export]
def dependencies(version : string) {
    require_package("das-minfft")
}

[export]
def build() {
    cmake_build()
}
