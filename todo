parser
    support initialization in global let
    support auto in let
    support late binding of default arguments
    support auto in function default arguments
    support auto in function arguments
    support ` (`debug "hello" "world")

misc
    support return
    basic type - test pointer casts
    points == null and pointer != null
    support 'clone' for variables, redo name lookup upstream during cloning?
    simplify builtin initialization
    support multi-dimensional array foreach
    support multi-dimensional array new (or regular array new)
    assert2 - need node, so that if ( !a ) b and not a,b, if
    &&, || - need nodes
    support (unsafe ?)

optimization \ ast massage
    flatten nested blocks
    eliminate dead code (after break and after return)
    flatten conditions
    support break and return to be last statement
    simplify initialization of local variables
    propagate local variable initialization on top of the function

// recent

iterations took:0.000657
foreach iterations took:0.000329
c++ version took:0.000017
interop version took:0.000498
foreach interop version took:0.000177
10000-interop version took:0.000225
ratio sim / c: 38.588715
ratio foreach sim / c: 19.299554
ratio interop / c: 29.260979
ratio foreach interop / c: 10.379814
ratio 10000-interop / c: 13.194692
ratio sim / interop: 1.318777



