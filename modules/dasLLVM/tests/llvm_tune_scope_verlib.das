options gen2

module llvm_tune_scope_verlib shared public

// Version-const fixture for llvm_tune_scope.das (no [test] on purpose — the suite just
// compiles it): the int constant llvm_tune_scope_client_verlib's version_of= pin names.
let TESTVER = 2
