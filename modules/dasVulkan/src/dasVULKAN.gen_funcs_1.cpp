// generated by generator/generate.das -- DO NOT EDIT

#include "dasVULKAN.h"

namespace das {

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkWaitForFences ( VkDevice device, uint32_t fenceCount, const VkFence * pFences, uint32_t waitAll, uint64_t timeout ) {
    return ::vkWaitForFences(device, fenceCount, pFences, waitAll, timeout);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateSemaphore ( VkDevice device, const VkSemaphoreCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkSemaphore & pSemaphore ) {
    return ::vkCreateSemaphore(device, &pCreateInfo, pAllocator, &pSemaphore);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroySemaphore ( VkDevice device, VkSemaphore semaphore, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroySemaphore(device, semaphore, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateEvent ( VkDevice device, const VkEventCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkEvent & pEvent ) {
    return ::vkCreateEvent(device, &pCreateInfo, pAllocator, &pEvent);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroyEvent ( VkDevice device, VkEvent event, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyEvent(device, event, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkGetEventStatus ( VkDevice device, VkEvent event ) {
    return ::vkGetEventStatus(device, event);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkSetEvent ( VkDevice device, VkEvent event ) {
    return ::vkSetEvent(device, event);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkResetEvent ( VkDevice device, VkEvent event ) {
    return ::vkResetEvent(device, event);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateQueryPool ( VkDevice device, const VkQueryPoolCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkQueryPool & pQueryPool ) {
    return ::vkCreateQueryPool(device, &pCreateInfo, pAllocator, &pQueryPool);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroyQueryPool ( VkDevice device, VkQueryPool queryPool, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyQueryPool(device, queryPool, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkGetQueryPoolResults ( VkDevice device, VkQueryPool queryPool, uint32_t firstQuery, uint32_t queryCount, uint64_t dataSize, void * pData, uint64_t stride, Bitfield flags ) {
    return ::vkGetQueryPoolResults(device, queryPool, firstQuery, queryCount, (size_t)dataSize, pData, stride, (VkQueryResultFlags)(flags.value));
}
#endif

#if defined(VK_VERSION_1_2)
static void WRAP_vkResetQueryPool ( VkDevice device, VkQueryPool queryPool, uint32_t firstQuery, uint32_t queryCount ) {
    ::vkResetQueryPool(device, queryPool, firstQuery, queryCount);
}
#endif

#if defined(VK_EXT_host_query_reset)
static void WRAP_vkResetQueryPoolEXT ( VkDevice device, VkQueryPool queryPool, uint32_t firstQuery, uint32_t queryCount ) {
    ::vkResetQueryPoolEXT(device, queryPool, firstQuery, queryCount);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateBuffer ( VkDevice device, const VkBufferCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkBuffer & pBuffer ) {
    return ::vkCreateBuffer(device, &pCreateInfo, pAllocator, &pBuffer);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroyBuffer ( VkDevice device, VkBuffer buffer, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyBuffer(device, buffer, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateBufferView ( VkDevice device, const VkBufferViewCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkBufferView & pView ) {
    return ::vkCreateBufferView(device, &pCreateInfo, pAllocator, &pView);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroyBufferView ( VkDevice device, VkBufferView bufferView, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyBufferView(device, bufferView, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateImage ( VkDevice device, const VkImageCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkImage & pImage ) {
    return ::vkCreateImage(device, &pCreateInfo, pAllocator, &pImage);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroyImage ( VkDevice device, VkImage image, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyImage(device, image, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkGetImageSubresourceLayout ( VkDevice device, VkImage image, const VkImageSubresource & pSubresource, VkSubresourceLayout & pLayout ) {
    ::vkGetImageSubresourceLayout(device, image, &pSubresource, &pLayout);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateImageView ( VkDevice device, const VkImageViewCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkImageView & pView ) {
    return ::vkCreateImageView(device, &pCreateInfo, pAllocator, &pView);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroyImageView ( VkDevice device, VkImageView imageView, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyImageView(device, imageView, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateShaderModule ( VkDevice device, const VkShaderModuleCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkShaderModule & pShaderModule ) {
    return ::vkCreateShaderModule(device, &pCreateInfo, pAllocator, &pShaderModule);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroyShaderModule ( VkDevice device, VkShaderModule shaderModule, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyShaderModule(device, shaderModule, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreatePipelineCache ( VkDevice device, const VkPipelineCacheCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkPipelineCache & pPipelineCache ) {
    return ::vkCreatePipelineCache(device, &pCreateInfo, pAllocator, &pPipelineCache);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroyPipelineCache ( VkDevice device, VkPipelineCache pipelineCache, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyPipelineCache(device, pipelineCache, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkGetPipelineCacheData ( VkDevice device, VkPipelineCache pipelineCache, uint64_t & pDataSize, void * pData ) {
    return ::vkGetPipelineCacheData(device, pipelineCache, (size_t *)&pDataSize, pData);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkMergePipelineCaches ( VkDevice device, VkPipelineCache dstCache, uint32_t srcCacheCount, const VkPipelineCache * pSrcCaches ) {
    return ::vkMergePipelineCaches(device, dstCache, srcCacheCount, pSrcCaches);
}
#endif

#if defined(VK_KHR_pipeline_binary)
static VkResult WRAP_vkCreatePipelineBinariesKHR ( VkDevice device, const VkPipelineBinaryCreateInfoKHR & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkPipelineBinaryHandlesInfoKHR & pBinaries ) {
    return ::vkCreatePipelineBinariesKHR(device, &pCreateInfo, pAllocator, &pBinaries);
}
#endif

#if defined(VK_KHR_pipeline_binary)
static void WRAP_vkDestroyPipelineBinaryKHR ( VkDevice device, VkPipelineBinaryKHR pipelineBinary, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyPipelineBinaryKHR(device, pipelineBinary, pAllocator);
}
#endif

#if defined(VK_KHR_pipeline_binary)
static VkResult WRAP_vkGetPipelineKeyKHR ( VkDevice device, const VkPipelineCreateInfoKHR * pPipelineCreateInfo, VkPipelineBinaryKeyKHR & pPipelineKey ) {
    return ::vkGetPipelineKeyKHR(device, pPipelineCreateInfo, &pPipelineKey);
}
#endif

#if defined(VK_KHR_pipeline_binary)
static VkResult WRAP_vkGetPipelineBinaryDataKHR ( VkDevice device, const VkPipelineBinaryDataInfoKHR & pInfo, VkPipelineBinaryKeyKHR & pPipelineBinaryKey, uint64_t & pPipelineBinaryDataSize, void * pPipelineBinaryData ) {
    return ::vkGetPipelineBinaryDataKHR(device, &pInfo, &pPipelineBinaryKey, (size_t *)&pPipelineBinaryDataSize, pPipelineBinaryData);
}
#endif

#if defined(VK_KHR_pipeline_binary)
static VkResult WRAP_vkReleaseCapturedPipelineDataKHR ( VkDevice device, const VkReleaseCapturedPipelineDataInfoKHR & pInfo, const VkAllocationCallbacks * pAllocator ) {
    return ::vkReleaseCapturedPipelineDataKHR(device, &pInfo, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateGraphicsPipelines ( VkDevice device, VkPipelineCache pipelineCache, uint32_t createInfoCount, const VkGraphicsPipelineCreateInfo * pCreateInfos, const VkAllocationCallbacks * pAllocator, VkPipeline * pPipelines ) {
    return ::vkCreateGraphicsPipelines(device, pipelineCache, createInfoCount, pCreateInfos, pAllocator, pPipelines);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateComputePipelines ( VkDevice device, VkPipelineCache pipelineCache, uint32_t createInfoCount, const VkComputePipelineCreateInfo * pCreateInfos, const VkAllocationCallbacks * pAllocator, VkPipeline * pPipelines ) {
    return ::vkCreateComputePipelines(device, pipelineCache, createInfoCount, pCreateInfos, pAllocator, pPipelines);
}
#endif

#if defined(VK_HUAWEI_subpass_shading)
static VkResult WRAP_vkGetDeviceSubpassShadingMaxWorkgroupSizeHUAWEI ( VkDevice device, VkRenderPass renderpass, VkExtent2D * pMaxWorkgroupSize ) {
    return ::vkGetDeviceSubpassShadingMaxWorkgroupSizeHUAWEI(device, renderpass, pMaxWorkgroupSize);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroyPipeline ( VkDevice device, VkPipeline pipeline, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyPipeline(device, pipeline, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreatePipelineLayout ( VkDevice device, const VkPipelineLayoutCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkPipelineLayout & pPipelineLayout ) {
    return ::vkCreatePipelineLayout(device, &pCreateInfo, pAllocator, &pPipelineLayout);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroyPipelineLayout ( VkDevice device, VkPipelineLayout pipelineLayout, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyPipelineLayout(device, pipelineLayout, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateSampler ( VkDevice device, const VkSamplerCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkSampler & pSampler ) {
    return ::vkCreateSampler(device, &pCreateInfo, pAllocator, &pSampler);
}
#endif

void das_vulkan_init_funcs_1(Module & mod, ModuleLibrary & lib) {
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkWaitForFences)>(mod, lib, "vkWaitForFences", SideEffects::modifyArgumentAndExternal, "WRAP_vkWaitForFences")
        ->args({"device","fenceCount","pFences","waitAll","timeout"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateSemaphore)>(mod, lib, "vkCreateSemaphore", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateSemaphore")
        ->args({"device","pCreateInfo","pAllocator","pSemaphore"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroySemaphore)>(mod, lib, "vkDestroySemaphore", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroySemaphore")
        ->args({"device","semaphore","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateEvent)>(mod, lib, "vkCreateEvent", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateEvent")
        ->args({"device","pCreateInfo","pAllocator","pEvent"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyEvent)>(mod, lib, "vkDestroyEvent", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyEvent")
        ->args({"device","event","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetEventStatus)>(mod, lib, "vkGetEventStatus", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetEventStatus")
        ->args({"device","event"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkSetEvent)>(mod, lib, "vkSetEvent", SideEffects::modifyArgumentAndExternal, "WRAP_vkSetEvent")
        ->args({"device","event"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkResetEvent)>(mod, lib, "vkResetEvent", SideEffects::modifyArgumentAndExternal, "WRAP_vkResetEvent")
        ->args({"device","event"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateQueryPool)>(mod, lib, "vkCreateQueryPool", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateQueryPool")
        ->args({"device","pCreateInfo","pAllocator","pQueryPool"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyQueryPool)>(mod, lib, "vkDestroyQueryPool", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyQueryPool")
        ->args({"device","queryPool","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetQueryPoolResults)>(mod, lib, "vkGetQueryPoolResults", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetQueryPoolResults")
        ->args({"device","queryPool","firstQuery","queryCount","dataSize","pData","stride","flags"});
#endif
#if defined(VK_VERSION_1_2)
    addExtern<DAS_BIND_FUN(WRAP_vkResetQueryPool)>(mod, lib, "vkResetQueryPool", SideEffects::modifyArgumentAndExternal, "WRAP_vkResetQueryPool")
        ->args({"device","queryPool","firstQuery","queryCount"});
#endif
#if defined(VK_EXT_host_query_reset)
    addExtern<DAS_BIND_FUN(WRAP_vkResetQueryPoolEXT)>(mod, lib, "vkResetQueryPoolEXT", SideEffects::modifyArgumentAndExternal, "WRAP_vkResetQueryPoolEXT")
        ->args({"device","queryPool","firstQuery","queryCount"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateBuffer)>(mod, lib, "vkCreateBuffer", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateBuffer")
        ->args({"device","pCreateInfo","pAllocator","pBuffer"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyBuffer)>(mod, lib, "vkDestroyBuffer", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyBuffer")
        ->args({"device","buffer","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateBufferView)>(mod, lib, "vkCreateBufferView", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateBufferView")
        ->args({"device","pCreateInfo","pAllocator","pView"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyBufferView)>(mod, lib, "vkDestroyBufferView", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyBufferView")
        ->args({"device","bufferView","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateImage)>(mod, lib, "vkCreateImage", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateImage")
        ->args({"device","pCreateInfo","pAllocator","pImage"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyImage)>(mod, lib, "vkDestroyImage", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyImage")
        ->args({"device","image","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetImageSubresourceLayout)>(mod, lib, "vkGetImageSubresourceLayout", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetImageSubresourceLayout")
        ->args({"device","image","pSubresource","pLayout"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateImageView)>(mod, lib, "vkCreateImageView", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateImageView")
        ->args({"device","pCreateInfo","pAllocator","pView"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyImageView)>(mod, lib, "vkDestroyImageView", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyImageView")
        ->args({"device","imageView","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateShaderModule)>(mod, lib, "vkCreateShaderModule", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateShaderModule")
        ->args({"device","pCreateInfo","pAllocator","pShaderModule"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyShaderModule)>(mod, lib, "vkDestroyShaderModule", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyShaderModule")
        ->args({"device","shaderModule","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreatePipelineCache)>(mod, lib, "vkCreatePipelineCache", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreatePipelineCache")
        ->args({"device","pCreateInfo","pAllocator","pPipelineCache"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyPipelineCache)>(mod, lib, "vkDestroyPipelineCache", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyPipelineCache")
        ->args({"device","pipelineCache","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetPipelineCacheData)>(mod, lib, "vkGetPipelineCacheData", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetPipelineCacheData")
        ->args({"device","pipelineCache","pDataSize","pData"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkMergePipelineCaches)>(mod, lib, "vkMergePipelineCaches", SideEffects::modifyArgumentAndExternal, "WRAP_vkMergePipelineCaches")
        ->args({"device","dstCache","srcCacheCount","pSrcCaches"});
#endif
#if defined(VK_KHR_pipeline_binary)
    addExtern<DAS_BIND_FUN(WRAP_vkCreatePipelineBinariesKHR)>(mod, lib, "vkCreatePipelineBinariesKHR", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreatePipelineBinariesKHR")
        ->args({"device","pCreateInfo","pAllocator","pBinaries"});
#endif
#if defined(VK_KHR_pipeline_binary)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyPipelineBinaryKHR)>(mod, lib, "vkDestroyPipelineBinaryKHR", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyPipelineBinaryKHR")
        ->args({"device","pipelineBinary","pAllocator"});
#endif
#if defined(VK_KHR_pipeline_binary)
    addExtern<DAS_BIND_FUN(WRAP_vkGetPipelineKeyKHR)>(mod, lib, "vkGetPipelineKeyKHR", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetPipelineKeyKHR")
        ->args({"device","pPipelineCreateInfo","pPipelineKey"});
#endif
#if defined(VK_KHR_pipeline_binary)
    addExtern<DAS_BIND_FUN(WRAP_vkGetPipelineBinaryDataKHR)>(mod, lib, "vkGetPipelineBinaryDataKHR", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetPipelineBinaryDataKHR")
        ->args({"device","pInfo","pPipelineBinaryKey","pPipelineBinaryDataSize","pPipelineBinaryData"});
#endif
#if defined(VK_KHR_pipeline_binary)
    addExtern<DAS_BIND_FUN(WRAP_vkReleaseCapturedPipelineDataKHR)>(mod, lib, "vkReleaseCapturedPipelineDataKHR", SideEffects::modifyArgumentAndExternal, "WRAP_vkReleaseCapturedPipelineDataKHR")
        ->args({"device","pInfo","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateGraphicsPipelines)>(mod, lib, "vkCreateGraphicsPipelines", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateGraphicsPipelines")
        ->args({"device","pipelineCache","createInfoCount","pCreateInfos","pAllocator","pPipelines"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateComputePipelines)>(mod, lib, "vkCreateComputePipelines", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateComputePipelines")
        ->args({"device","pipelineCache","createInfoCount","pCreateInfos","pAllocator","pPipelines"});
#endif
#if defined(VK_HUAWEI_subpass_shading)
    addExtern<DAS_BIND_FUN(WRAP_vkGetDeviceSubpassShadingMaxWorkgroupSizeHUAWEI)>(mod, lib, "vkGetDeviceSubpassShadingMaxWorkgroupSizeHUAWEI", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetDeviceSubpassShadingMaxWorkgroupSizeHUAWEI")
        ->args({"device","renderpass","pMaxWorkgroupSize"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyPipeline)>(mod, lib, "vkDestroyPipeline", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyPipeline")
        ->args({"device","pipeline","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreatePipelineLayout)>(mod, lib, "vkCreatePipelineLayout", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreatePipelineLayout")
        ->args({"device","pCreateInfo","pAllocator","pPipelineLayout"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyPipelineLayout)>(mod, lib, "vkDestroyPipelineLayout", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyPipelineLayout")
        ->args({"device","pipelineLayout","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateSampler)>(mod, lib, "vkCreateSampler", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateSampler")
        ->args({"device","pCreateInfo","pAllocator","pSampler"});
#endif
}

}
