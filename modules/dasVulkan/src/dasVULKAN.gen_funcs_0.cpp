// generated by generator/generate.das -- DO NOT EDIT

#include "dasVULKAN.h"

namespace das {

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateInstance ( const VkInstanceCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkInstance & pInstance ) {
    return ::vkCreateInstance(&pCreateInfo, pAllocator, &pInstance);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroyInstance ( VkInstance instance, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyInstance(instance, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkEnumeratePhysicalDevices ( VkInstance instance, uint32_t & pPhysicalDeviceCount, VkPhysicalDevice * pPhysicalDevices ) {
    return ::vkEnumeratePhysicalDevices(instance, (uint32_t *)&pPhysicalDeviceCount, pPhysicalDevices);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkGetPhysicalDeviceProperties ( VkPhysicalDevice physicalDevice, VkPhysicalDeviceProperties & pProperties ) {
    ::vkGetPhysicalDeviceProperties(physicalDevice, &pProperties);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkGetPhysicalDeviceQueueFamilyProperties ( VkPhysicalDevice physicalDevice, uint32_t & pQueueFamilyPropertyCount, VkQueueFamilyProperties * pQueueFamilyProperties ) {
    ::vkGetPhysicalDeviceQueueFamilyProperties(physicalDevice, (uint32_t *)&pQueueFamilyPropertyCount, pQueueFamilyProperties);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkGetPhysicalDeviceMemoryProperties ( VkPhysicalDevice physicalDevice, VkPhysicalDeviceMemoryProperties & pMemoryProperties ) {
    ::vkGetPhysicalDeviceMemoryProperties(physicalDevice, &pMemoryProperties);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkGetPhysicalDeviceFeatures ( VkPhysicalDevice physicalDevice, VkPhysicalDeviceFeatures & pFeatures ) {
    ::vkGetPhysicalDeviceFeatures(physicalDevice, &pFeatures);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkGetPhysicalDeviceFormatProperties ( VkPhysicalDevice physicalDevice, VkFormat format, VkFormatProperties & pFormatProperties ) {
    ::vkGetPhysicalDeviceFormatProperties(physicalDevice, format, &pFormatProperties);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkGetPhysicalDeviceImageFormatProperties ( VkPhysicalDevice physicalDevice, VkFormat format, VkImageType type, VkImageTiling tiling, Bitfield usage, Bitfield flags, VkImageFormatProperties & pImageFormatProperties ) {
    return ::vkGetPhysicalDeviceImageFormatProperties(physicalDevice, format, type, tiling, (VkImageUsageFlags)(usage.value), (VkImageCreateFlags)(flags.value), &pImageFormatProperties);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateDevice ( VkPhysicalDevice physicalDevice, const VkDeviceCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkDevice & pDevice ) {
    return ::vkCreateDevice(physicalDevice, &pCreateInfo, pAllocator, &pDevice);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroyDevice ( VkDevice device, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyDevice(device, pAllocator);
}
#endif

#if defined(VK_VERSION_1_1)
static VkResult WRAP_vkEnumerateInstanceVersion ( uint32_t & pApiVersion ) {
    return ::vkEnumerateInstanceVersion((uint32_t *)&pApiVersion);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkEnumerateInstanceLayerProperties ( uint32_t & pPropertyCount, VkLayerProperties * pProperties ) {
    return ::vkEnumerateInstanceLayerProperties((uint32_t *)&pPropertyCount, pProperties);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkEnumerateInstanceExtensionProperties ( const char * pLayerName, uint32_t & pPropertyCount, VkExtensionProperties * pProperties ) {
    return ::vkEnumerateInstanceExtensionProperties(pLayerName, (uint32_t *)&pPropertyCount, pProperties);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkEnumerateDeviceLayerProperties ( VkPhysicalDevice physicalDevice, uint32_t & pPropertyCount, VkLayerProperties * pProperties ) {
    return ::vkEnumerateDeviceLayerProperties(physicalDevice, (uint32_t *)&pPropertyCount, pProperties);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkEnumerateDeviceExtensionProperties ( VkPhysicalDevice physicalDevice, const char * pLayerName, uint32_t & pPropertyCount, VkExtensionProperties * pProperties ) {
    return ::vkEnumerateDeviceExtensionProperties(physicalDevice, pLayerName, (uint32_t *)&pPropertyCount, pProperties);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkGetDeviceQueue ( VkDevice device, uint32_t queueFamilyIndex, uint32_t queueIndex, VkQueue & pQueue ) {
    ::vkGetDeviceQueue(device, queueFamilyIndex, queueIndex, &pQueue);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkQueueSubmit ( VkQueue queue, uint32_t submitCount, const VkSubmitInfo * pSubmits, VkFence fence ) {
    return ::vkQueueSubmit(queue, submitCount, pSubmits, fence);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkQueueWaitIdle ( VkQueue queue ) {
    return ::vkQueueWaitIdle(queue);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkDeviceWaitIdle ( VkDevice device ) {
    return ::vkDeviceWaitIdle(device);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkAllocateMemory ( VkDevice device, const VkMemoryAllocateInfo & pAllocateInfo, const VkAllocationCallbacks * pAllocator, VkDeviceMemory & pMemory ) {
    return ::vkAllocateMemory(device, &pAllocateInfo, pAllocator, &pMemory);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkFreeMemory ( VkDevice device, VkDeviceMemory memory, const VkAllocationCallbacks * pAllocator ) {
    ::vkFreeMemory(device, memory, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkMapMemory ( VkDevice device, VkDeviceMemory memory, uint64_t offset, uint64_t size, Bitfield flags, void ** ppData ) {
    return ::vkMapMemory(device, memory, offset, size, (VkMemoryMapFlags)(flags.value), ppData);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkUnmapMemory ( VkDevice device, VkDeviceMemory memory ) {
    ::vkUnmapMemory(device, memory);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkFlushMappedMemoryRanges ( VkDevice device, uint32_t memoryRangeCount, const VkMappedMemoryRange * pMemoryRanges ) {
    return ::vkFlushMappedMemoryRanges(device, memoryRangeCount, pMemoryRanges);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkInvalidateMappedMemoryRanges ( VkDevice device, uint32_t memoryRangeCount, const VkMappedMemoryRange * pMemoryRanges ) {
    return ::vkInvalidateMappedMemoryRanges(device, memoryRangeCount, pMemoryRanges);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkGetDeviceMemoryCommitment ( VkDevice device, VkDeviceMemory memory, uint64_t & pCommittedMemoryInBytes ) {
    ::vkGetDeviceMemoryCommitment(device, memory, (VkDeviceSize *)&pCommittedMemoryInBytes);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkGetBufferMemoryRequirements ( VkDevice device, VkBuffer buffer, VkMemoryRequirements & pMemoryRequirements ) {
    ::vkGetBufferMemoryRequirements(device, buffer, &pMemoryRequirements);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkBindBufferMemory ( VkDevice device, VkBuffer buffer, VkDeviceMemory memory, uint64_t memoryOffset ) {
    return ::vkBindBufferMemory(device, buffer, memory, memoryOffset);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkGetImageMemoryRequirements ( VkDevice device, VkImage image, VkMemoryRequirements & pMemoryRequirements ) {
    ::vkGetImageMemoryRequirements(device, image, &pMemoryRequirements);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkBindImageMemory ( VkDevice device, VkImage image, VkDeviceMemory memory, uint64_t memoryOffset ) {
    return ::vkBindImageMemory(device, image, memory, memoryOffset);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkGetImageSparseMemoryRequirements ( VkDevice device, VkImage image, uint32_t & pSparseMemoryRequirementCount, VkSparseImageMemoryRequirements * pSparseMemoryRequirements ) {
    ::vkGetImageSparseMemoryRequirements(device, image, (uint32_t *)&pSparseMemoryRequirementCount, pSparseMemoryRequirements);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkGetPhysicalDeviceSparseImageFormatProperties ( VkPhysicalDevice physicalDevice, VkFormat format, VkImageType type, VkSampleCountFlagBits samples, Bitfield usage, VkImageTiling tiling, uint32_t & pPropertyCount, VkSparseImageFormatProperties * pProperties ) {
    ::vkGetPhysicalDeviceSparseImageFormatProperties(physicalDevice, format, type, samples, (VkImageUsageFlags)(usage.value), tiling, (uint32_t *)&pPropertyCount, pProperties);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkQueueBindSparse ( VkQueue queue, uint32_t bindInfoCount, const VkBindSparseInfo * pBindInfo, VkFence fence ) {
    return ::vkQueueBindSparse(queue, bindInfoCount, pBindInfo, fence);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkCreateFence ( VkDevice device, const VkFenceCreateInfo & pCreateInfo, const VkAllocationCallbacks * pAllocator, VkFence & pFence ) {
    return ::vkCreateFence(device, &pCreateInfo, pAllocator, &pFence);
}
#endif

#if defined(VK_VERSION_1_0)
static void WRAP_vkDestroyFence ( VkDevice device, VkFence fence, const VkAllocationCallbacks * pAllocator ) {
    ::vkDestroyFence(device, fence, pAllocator);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkResetFences ( VkDevice device, uint32_t fenceCount, const VkFence * pFences ) {
    return ::vkResetFences(device, fenceCount, pFences);
}
#endif

#if defined(VK_VERSION_1_0)
static VkResult WRAP_vkGetFenceStatus ( VkDevice device, VkFence fence ) {
    return ::vkGetFenceStatus(device, fence);
}
#endif

void das_vulkan_init_funcs_0(Module & mod, ModuleLibrary & lib) {
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateInstance)>(mod, lib, "vkCreateInstance", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateInstance")
        ->args({"pCreateInfo","pAllocator","pInstance"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyInstance)>(mod, lib, "vkDestroyInstance", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyInstance")
        ->args({"instance","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkEnumeratePhysicalDevices)>(mod, lib, "vkEnumeratePhysicalDevices", SideEffects::modifyArgumentAndExternal, "WRAP_vkEnumeratePhysicalDevices")
        ->args({"instance","pPhysicalDeviceCount","pPhysicalDevices"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetPhysicalDeviceProperties)>(mod, lib, "vkGetPhysicalDeviceProperties", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetPhysicalDeviceProperties")
        ->args({"physicalDevice","pProperties"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetPhysicalDeviceQueueFamilyProperties)>(mod, lib, "vkGetPhysicalDeviceQueueFamilyProperties", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetPhysicalDeviceQueueFamilyProperties")
        ->args({"physicalDevice","pQueueFamilyPropertyCount","pQueueFamilyProperties"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetPhysicalDeviceMemoryProperties)>(mod, lib, "vkGetPhysicalDeviceMemoryProperties", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetPhysicalDeviceMemoryProperties")
        ->args({"physicalDevice","pMemoryProperties"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetPhysicalDeviceFeatures)>(mod, lib, "vkGetPhysicalDeviceFeatures", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetPhysicalDeviceFeatures")
        ->args({"physicalDevice","pFeatures"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetPhysicalDeviceFormatProperties)>(mod, lib, "vkGetPhysicalDeviceFormatProperties", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetPhysicalDeviceFormatProperties")
        ->args({"physicalDevice","format","pFormatProperties"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetPhysicalDeviceImageFormatProperties)>(mod, lib, "vkGetPhysicalDeviceImageFormatProperties", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetPhysicalDeviceImageFormatProperties")
        ->args({"physicalDevice","format","type_","tiling","usage","flags","pImageFormatProperties"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateDevice)>(mod, lib, "vkCreateDevice", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateDevice")
        ->args({"physicalDevice","pCreateInfo","pAllocator","pDevice"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyDevice)>(mod, lib, "vkDestroyDevice", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyDevice")
        ->args({"device","pAllocator"});
#endif
#if defined(VK_VERSION_1_1)
    addExtern<DAS_BIND_FUN(WRAP_vkEnumerateInstanceVersion)>(mod, lib, "vkEnumerateInstanceVersion", SideEffects::modifyArgumentAndExternal, "WRAP_vkEnumerateInstanceVersion")
        ->args({"pApiVersion"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkEnumerateInstanceLayerProperties)>(mod, lib, "vkEnumerateInstanceLayerProperties", SideEffects::modifyArgumentAndExternal, "WRAP_vkEnumerateInstanceLayerProperties")
        ->args({"pPropertyCount","pProperties"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkEnumerateInstanceExtensionProperties)>(mod, lib, "vkEnumerateInstanceExtensionProperties", SideEffects::modifyArgumentAndExternal, "WRAP_vkEnumerateInstanceExtensionProperties")
        ->args({"pLayerName","pPropertyCount","pProperties"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkEnumerateDeviceLayerProperties)>(mod, lib, "vkEnumerateDeviceLayerProperties", SideEffects::modifyArgumentAndExternal, "WRAP_vkEnumerateDeviceLayerProperties")
        ->args({"physicalDevice","pPropertyCount","pProperties"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkEnumerateDeviceExtensionProperties)>(mod, lib, "vkEnumerateDeviceExtensionProperties", SideEffects::modifyArgumentAndExternal, "WRAP_vkEnumerateDeviceExtensionProperties")
        ->args({"physicalDevice","pLayerName","pPropertyCount","pProperties"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetDeviceQueue)>(mod, lib, "vkGetDeviceQueue", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetDeviceQueue")
        ->args({"device","queueFamilyIndex","queueIndex","pQueue"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkQueueSubmit)>(mod, lib, "vkQueueSubmit", SideEffects::modifyArgumentAndExternal, "WRAP_vkQueueSubmit")
        ->args({"queue","submitCount","pSubmits","fence"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkQueueWaitIdle)>(mod, lib, "vkQueueWaitIdle", SideEffects::modifyArgumentAndExternal, "WRAP_vkQueueWaitIdle")
        ->args({"queue"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDeviceWaitIdle)>(mod, lib, "vkDeviceWaitIdle", SideEffects::modifyArgumentAndExternal, "WRAP_vkDeviceWaitIdle")
        ->args({"device"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkAllocateMemory)>(mod, lib, "vkAllocateMemory", SideEffects::modifyArgumentAndExternal, "WRAP_vkAllocateMemory")
        ->args({"device","pAllocateInfo","pAllocator","pMemory"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkFreeMemory)>(mod, lib, "vkFreeMemory", SideEffects::modifyArgumentAndExternal, "WRAP_vkFreeMemory")
        ->args({"device","memory","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkMapMemory)>(mod, lib, "vkMapMemory", SideEffects::modifyArgumentAndExternal, "WRAP_vkMapMemory")
        ->args({"device","memory","offset","size","flags","ppData"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkUnmapMemory)>(mod, lib, "vkUnmapMemory", SideEffects::modifyArgumentAndExternal, "WRAP_vkUnmapMemory")
        ->args({"device","memory"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkFlushMappedMemoryRanges)>(mod, lib, "vkFlushMappedMemoryRanges", SideEffects::modifyArgumentAndExternal, "WRAP_vkFlushMappedMemoryRanges")
        ->args({"device","memoryRangeCount","pMemoryRanges"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkInvalidateMappedMemoryRanges)>(mod, lib, "vkInvalidateMappedMemoryRanges", SideEffects::modifyArgumentAndExternal, "WRAP_vkInvalidateMappedMemoryRanges")
        ->args({"device","memoryRangeCount","pMemoryRanges"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetDeviceMemoryCommitment)>(mod, lib, "vkGetDeviceMemoryCommitment", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetDeviceMemoryCommitment")
        ->args({"device","memory","pCommittedMemoryInBytes"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetBufferMemoryRequirements)>(mod, lib, "vkGetBufferMemoryRequirements", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetBufferMemoryRequirements")
        ->args({"device","buffer","pMemoryRequirements"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkBindBufferMemory)>(mod, lib, "vkBindBufferMemory", SideEffects::modifyArgumentAndExternal, "WRAP_vkBindBufferMemory")
        ->args({"device","buffer","memory","memoryOffset"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetImageMemoryRequirements)>(mod, lib, "vkGetImageMemoryRequirements", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetImageMemoryRequirements")
        ->args({"device","image","pMemoryRequirements"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkBindImageMemory)>(mod, lib, "vkBindImageMemory", SideEffects::modifyArgumentAndExternal, "WRAP_vkBindImageMemory")
        ->args({"device","image","memory","memoryOffset"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetImageSparseMemoryRequirements)>(mod, lib, "vkGetImageSparseMemoryRequirements", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetImageSparseMemoryRequirements")
        ->args({"device","image","pSparseMemoryRequirementCount","pSparseMemoryRequirements"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetPhysicalDeviceSparseImageFormatProperties)>(mod, lib, "vkGetPhysicalDeviceSparseImageFormatProperties", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetPhysicalDeviceSparseImageFormatProperties")
        ->args({"physicalDevice","format","type_","samples","usage","tiling","pPropertyCount","pProperties"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkQueueBindSparse)>(mod, lib, "vkQueueBindSparse", SideEffects::modifyArgumentAndExternal, "WRAP_vkQueueBindSparse")
        ->args({"queue","bindInfoCount","pBindInfo","fence"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkCreateFence)>(mod, lib, "vkCreateFence", SideEffects::modifyArgumentAndExternal, "WRAP_vkCreateFence")
        ->args({"device","pCreateInfo","pAllocator","pFence"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkDestroyFence)>(mod, lib, "vkDestroyFence", SideEffects::modifyArgumentAndExternal, "WRAP_vkDestroyFence")
        ->args({"device","fence","pAllocator"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkResetFences)>(mod, lib, "vkResetFences", SideEffects::modifyArgumentAndExternal, "WRAP_vkResetFences")
        ->args({"device","fenceCount","pFences"});
#endif
#if defined(VK_VERSION_1_0)
    addExtern<DAS_BIND_FUN(WRAP_vkGetFenceStatus)>(mod, lib, "vkGetFenceStatus", SideEffects::modifyArgumentAndExternal, "WRAP_vkGetFenceStatus")
        ->args({"device","fence"});
#endif
}

}
