# dasVulkan Generator Code Review Checklist

**Read `REVIEW_COMMON.md` (repo root) first — its contract binds this checklist.** Architecture
doc: `../CLAUDE.md`.

**A diff that grows `skip_report.txt` states in its commit message why the newly skipped surface
is acceptable.** CI checks only that the committed report matches a fresh run; whether losing that
bound surface is intended is the reviewer's call.
