# corpus is fetched/regenerated by performance/setup_asr_rig.das — corpus.sha256 is the
# committed truth; the wavs, .sha sidecars and the librispeech subset never enter the repo
corpus/
results_raw.tsv
baseline_*.log
.venv-*/
