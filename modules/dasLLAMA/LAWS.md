# LAWS - append-only intent provenance for this folder's rule documents

- **2026-08-24** (`REVIEW.md`, `REVIEW.das`): Boris asked for a REVIEW.das check for dasLLAMA
  that fires if it finds the upstream reference engine's project name anywhere in `.das`
  comments or `.md` files - "we really don't want to advertise 'zomg we took it verbatim'
  anywhere. for anything. like 'this <name> kernel dominates' becomes 'this kernel dominates'"
  - "including REVIEW.das itself"; on seeing the existing mention count: "my bad. i did not
  realize we do that a lot. i just did. it stops now."
