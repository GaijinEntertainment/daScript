This bitfield specifies how exactly values are to be printed
if string is to be escaped
names of the fields and dimensions of the arrays
type qualifiers for the specific types like double and uint64
addresses in hexadecimal of each reference value
human readable vs single line
always output fixed point precision for floating point values
