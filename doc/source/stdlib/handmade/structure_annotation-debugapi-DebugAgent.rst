Annotation for declaring a debug agent class. Generates the boilerplate to subclass `DebugAgent` from C++. The optional `isThreadLocal` field installs as a thread-local agent.
True if the debug agent is installed per-thread rather than globally.
