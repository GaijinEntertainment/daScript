Base class for creating reader macros via the ``[reader_macro]`` structure annotation; registers an ``AstReaderMacro`` with the compiler.
Name of the builtin function used to register this annotation with the compiler.
