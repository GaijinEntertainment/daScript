The slot the global ``variable`` holds in ``program``'s context: ``-1`` when the program
never allocated it, ``-2`` when the allocation found it unused, its index in the
context's global table otherwise.
