Implements TFlatHashTable template (typemacro), similar to regular table.
Key can be any type, for which 'hash' function is defined, and equality operator is defined. Value can be any type.

All functions and symbols are in "flat_hash_table" module, use require to get access to it. ::

    require daslib/flat_hash_table