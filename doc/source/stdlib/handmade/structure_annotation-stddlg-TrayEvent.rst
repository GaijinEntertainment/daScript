One tray event as handed to the tray_poll block.
Which kind of event this is.
For a menu event, the id of the chosen entry; zero otherwise.
Screen x of the pointer when the host reports it (top-left origin), zero otherwise.
Screen y of the pointer when the host reports it (top-left origin), zero otherwise.
