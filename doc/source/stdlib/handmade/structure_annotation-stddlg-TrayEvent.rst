One tray event as handed to the tray_poll block.
Which kind of event this is.
For a menu event, the id of the chosen entry; zero otherwise.
Pointer x in the host's screen units (pixels on Windows, points on macOS, the panel's numbers on Linux), top-left origin, when the host reports it; zero otherwise.
Pointer y in the same units and origin as x; zero otherwise.
