The ``[pre_infer_macro]`` structure annotation — registers an ``AstPassMacro`` subclass that is invoked before every type inference pass, on a tree a macro may have just modified. Override ``canVisitPass(prog, mod, index)`` and return ``false`` to skip a pass; ``index`` is the pass number within the current inference run, 0 after every (re)start.