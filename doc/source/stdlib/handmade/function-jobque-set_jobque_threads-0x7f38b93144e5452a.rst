Requests the TOTAL compute lane count of job queues created afterwards - N-1 workers plus the computing main thread. Unlike the cap, a request can raise past the platform default, which is what a batch workload needs to reach a hybrid box's second compute tier; the cap still bounds an accepted request, and the DAS_JOBQUE_THREADS environment variable overrides everything. A value of 1 or less clears the request.
