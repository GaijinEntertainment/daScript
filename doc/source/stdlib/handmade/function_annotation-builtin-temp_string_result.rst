Declares that the function's result is always a fresh string allocation (or null) - never a passthrough of an input, never an interned or cached string, never retained by the callee. Lets the temp-string reclaim pass queue the result on the 1-slot dispose queue when it dies in the consuming call. The return type must be a string; the propagation pass also derives this flag automatically for functions whose every return is a fresh producer.
