Base class for creating post-rewrite pass macros via the ``[post_rewrite_macro]`` structure annotation; registers an ``AstPassMacro`` that runs right after any user pass macro reports that it rewrote the tree.
Name of the builtin function used to register this annotation with the compiler.
