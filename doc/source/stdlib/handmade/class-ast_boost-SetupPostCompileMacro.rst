Base class for creating post-compile pass macros via the ``[post_compile_macro]`` structure annotation; registers an ``AstPassMacro`` that runs once the module is compiled and its gc root collected.
Name of the builtin function used to register this annotation with the compiler.
