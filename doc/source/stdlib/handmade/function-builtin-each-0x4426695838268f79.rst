Iterates over values, which are supplied via lambda.
When lambda returns false - no more values are iterated.
:param lam: lambda which takes reference to the value we iterate over, and returns bool
:returns: iterator
