Stored captured variable together with the `ExprVar` which uses it
captured variable
expression which uses the variable
this one indicates if its used by reference and does not come from argument. its only used in JIT
