.. _embedding_nano:

.. index::
   single: Embedding; daslang-nano
   single: Embedding; Embedded Targets
   single: Embedding; libDaScriptNano
   single: daslang-nano

.. This page has NO content of its own. The single source is nano/README.md,
   which sits beside the code it describes and ships with the SDK, so daslang.io
   and the shipped runtime can never disagree. Edit the .md.

.. include:: ../../../../nano/README.md
   :parser: myst_parser.sphinx_
