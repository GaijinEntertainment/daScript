.. _utils_detect_dupe:

.. This page has NO content of its own. The single source is the markdown file
   skills/detect_dupe_reference.md, which also ships in the SDK bundle (it is named
   in install/skills.list), so daslang.io and the SDK can never disagree.
   NOTE the name: skills/detect_dupe.md is the separate, shorter agent-facing skill.
   This is the long-form user reference it points at.
.. The page label is declared HERE rather than in the .md: a MyST target of the
   form (label)= does NOT register in Sphinx's std domain when the markdown is
   pulled in through `.. include:: ... :parser:`.

.. include:: ../../../../skills/detect_dupe_reference.md
   :parser: myst_parser.sphinx_
