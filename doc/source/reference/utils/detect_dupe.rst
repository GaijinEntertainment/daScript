.. _utils_detect_dupe:

.. index::
   single: Utils; detect-dupe
   single: Utils; Duplicate detection
   single: Utils; Code similarity

.. This page has NO content of its own. The single source is the markdown file
   skills/internal/detect_dupe_reference.md, so daslang.io and the repo can never
   disagree. NOTE the name: skills/internal/detect_dupe.md is the separate, shorter
   agent-facing skill. This is the long-form user reference it points at.
.. The page label is declared HERE rather than in the .md: a MyST target of the
   form (label)= does NOT register in Sphinx's std domain when the markdown is
   pulled in through `.. include:: ... :parser:`.

.. include:: ../../../../skills/internal/detect_dupe_reference.md
   :parser: myst_parser.sphinx_
