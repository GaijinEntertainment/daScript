(let
    (int 100 objects)
)

(defun (void test)
    (let
        (int count 0)
        (int index 0)
        (while (< index 1000)
            (
                (try
                    (@ objects index)
                    (+= count 1)
                )
                (+= index 1)
            )
        )
    )
)
