// GENERATED by utils/internal/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/internal/dasgen/gen_type_conformance.das
options gen2
options _function_length = 0
require dastest/testing_boost public
require daslib/archive

struct Wrap_uint16 {
    pad : uint8
    v : uint16
}

def roundtrip_uint16(v : uint16) : uint16 => v

[test]
def test_uint16_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<uint16>), 2)
        t |> equal(typeinfo alignof(type<uint16>), 2)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : uint16
        t |> success(z == uint16(0u), "zero init")
        t |> success(default<uint16> == uint16(0u), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = uint16(1u)
        let b = uint16(1u)
        let c = uint16(2u)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = uint16(1u)
        var c = a
        c = uint16(5u)
        t |> success(a == uint16(1u), "source unchanged")
        t |> success(c == uint16(5u), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = uint16(1u)
        t |> success(roundtrip_uint16(a) == a, "roundtrip")
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_uint16(pad = uint8(1), v = uint16(1u))
        t |> success(w.v == uint16(1u), "field read")
        w.v = uint16(5u)
        t |> success(w.v == uint16(5u), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_uint16>), 4)
    }
    t |> run("array element") @(t : T?) {
        var arr <- [uint16(1u), uint16(5u)]
        t |> equal(length(arr), 2)
        t |> success(arr[0] == uint16(1u), "array read 0")
        t |> success(arr[1] == uint16(5u), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = uint16(1u)
        var data <- mem_archive_save(a)
        var back : uint16
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = uint16(1u)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
}
