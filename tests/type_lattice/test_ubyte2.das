// GENERATED by utils/internal/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/internal/dasgen/gen_type_conformance.das
options gen2
options _function_length = 0
require dastest/testing_boost public
require daslib/archive

struct Wrap_ubyte2 {
    pad : uint8
    v : ubyte2
}

def roundtrip_ubyte2(v : ubyte2) : ubyte2 => v

[test]
def test_ubyte2_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<ubyte2>), 2)
        t |> equal(typeinfo alignof(type<ubyte2>), 1)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : ubyte2
        t |> success(z == ubyte2(0u, 0u), "zero init")
        t |> success(default<ubyte2> == ubyte2(0u, 0u), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = ubyte2(1u, 2u)
        let b = ubyte2(1u, 2u)
        let c = ubyte2(2u, 3u)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
        t |> success(ubyte2(3u) == ubyte2(3u, 3u), "splat ctor")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = ubyte2(1u, 2u)
        var c = a
        c = ubyte2(5u, 6u)
        t |> success(a == ubyte2(1u, 2u), "source unchanged")
        t |> success(c == ubyte2(5u, 6u), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = ubyte2(1u, 2u)
        t |> success(roundtrip_ubyte2(a) == a, "roundtrip")
    }
    t |> run("index read and write") @(t : T?) {
        var a = ubyte2(1u, 2u)
        t |> equal(a[0], uint8(1))
        t |> equal(a[1], uint8(2))
        a[1] = uint8(9)
        t |> equal(a[1], uint8(9))
    }
    t |> run("swizzle") @(t : T?) {
        var a = ubyte2(1u, 2u)
        t |> equal(a.x, uint8(1))
        t |> equal(a.y, uint8(2))
        t |> success(a.yx == ubyte2(2u, 1u), "reverse read")
        a.y = uint8(9)
        t |> equal(a.y, uint8(9))
        a.xy = ubyte2(7u, 8u)
        t |> equal(a.x, uint8(7))
        t |> equal(a.y, uint8(8))
    }
    t |> run("s-namespace swizzle") @(t : T?) {
        var a = ubyte2(1u, 2u)
        t |> equal(a.s1, uint8(2))
        t |> success(a.s10 == ubyte2(2u, 1u), "pair reorder")
        a.s0 = uint8(9)
        t |> equal(a.s0, uint8(9))
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_ubyte2(pad = uint8(1), v = ubyte2(1u, 2u))
        t |> success(w.v == ubyte2(1u, 2u), "field read")
        w.v = ubyte2(5u, 6u)
        t |> success(w.v == ubyte2(5u, 6u), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_ubyte2>), 3)
    }
    t |> run("array element") @(t : T?) {
        var arr <- [ubyte2(1u, 2u), ubyte2(5u, 6u)]
        t |> equal(length(arr), 2)
        t |> success(arr[0] == ubyte2(1u, 2u), "array read 0")
        t |> success(arr[1] == ubyte2(5u, 6u), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = ubyte2(1u, 2u)
        var data <- mem_archive_save(a)
        var back : ubyte2
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = ubyte2(1u, 2u)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
    t |> run("shift right") @(t : T?) {
        let a = ubyte2(200u, 203u)
        t |> success(a >> 2 == ubyte2(50u, 50u), "logical shift")
        t |> success(a >> 10 == a >> 2, "count masks to lane width")
        t |> success(a >> 0 == a, "shift by zero")
        var c = a
        c >>= 2
        t |> success(c == a >> 2, "shift right assign")
    }
    t |> run("shift left") @(t : T?) {
        let a = ubyte2(200u, 203u)
        t |> success(a << 2 == ubyte2(32u, 44u), "lanes wrap")
        t |> success(a << 10 == a << 2, "count masks to lane width")
        var c = a
        c <<= 2
        t |> success(c == a << 2, "shift left assign")
    }
    t |> run("bitwise") @(t : T?) {
        let a = ubyte2(200u, 203u)
        let b = ubyte2(45u, 50u)
        t |> success((a & b) == ubyte2(8u, 2u), "and")
        t |> success((a | b) == ubyte2(237u, 251u), "or")
        t |> success((a ^ b) == ubyte2(229u, 249u), "xor")
        var c = a
        c &= b
        t |> success(c == (a & b), "and assign")
        c = a
        c |= b
        t |> success(c == (a | b), "or assign")
        c = a
        c ^= b
        t |> success(c == (a ^ b), "xor assign")
    }
    t |> run("converts") @(t : T?) {
        let a = ubyte2(1u, 2u)
        let w = uint2(a)
        t |> success(w == uint2(1u, 2u), "widen")
        t |> success(ubyte2(w) == a, "narrow round trip")
        let s = ubyte2_sat(uint2(100000u, 1u))
        t |> success(s == ubyte2(255u, 1u), "sat clamp")
    }
}
