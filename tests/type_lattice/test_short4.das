// GENERATED by utils/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/dasgen/gen_type_conformance.das
options gen2
require dastest/testing_boost public
require daslib/archive

struct Wrap_short4 {
    pad : uint8
    v : short4
}

def roundtrip_short4(v : short4) : short4 => v

[test]
def test_short4_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<short4>), 8)
        t |> equal(typeinfo alignof(type<short4>), 2)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : short4
        t |> success(z == short4(0, 0, 0, 0), "zero init")
        t |> success(default<short4> == short4(0, 0, 0, 0), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = short4(1, 2, 3, 4)
        let b = short4(1, 2, 3, 4)
        let c = short4(2, 3, 4, 5)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
        t |> success(short4(3) == short4(3, 3, 3, 3), "splat ctor")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = short4(1, 2, 3, 4)
        var c = a
        c = short4(5, 6, 7, 8)
        t |> success(a == short4(1, 2, 3, 4), "source unchanged")
        t |> success(c == short4(5, 6, 7, 8), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = short4(1, 2, 3, 4)
        t |> success(roundtrip_short4(a) == a, "roundtrip")
    }
    t |> run("index read and write") @(t : T?) {
        var a = short4(1, 2, 3, 4)
        t |> equal(a[0], int16(1))
        t |> equal(a[1], int16(2))
        t |> equal(a[2], int16(3))
        t |> equal(a[3], int16(4))
        a[3] = int16(9)
        t |> equal(a[3], int16(9))
    }
    t |> run("swizzle") @(t : T?) {
        var a = short4(1, 2, 3, 4)
        t |> equal(a.x, int16(1))
        t |> equal(a.w, int16(4))
        t |> success(a.wzyx == short4(4, 3, 2, 1), "reverse read")
        t |> success(a.xy == short2(1, 2), "pair read")
        a.w = int16(9)
        t |> equal(a.w, int16(9))
        a.xy = short2(7, 8)
        t |> equal(a.x, int16(7))
        t |> equal(a.y, int16(8))
    }
    t |> run("s-namespace swizzle") @(t : T?) {
        var a = short4(1, 2, 3, 4)
        t |> equal(a.s3, int16(4))
        t |> success(a.s30 == short2(4, 1), "pair reorder")
        a.s0 = int16(9)
        t |> equal(a.s0, int16(9))
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_short4(pad = uint8(1), v = short4(1, 2, 3, 4))
        t |> success(w.v == short4(1, 2, 3, 4), "field read")
        w.v = short4(5, 6, 7, 8)
        t |> success(w.v == short4(5, 6, 7, 8), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_short4>), 10)
    }
    t |> run("array element") @(t : T?) {
        var arr : array<short4>
        arr |> push(short4(1, 2, 3, 4))
        arr |> push(short4(5, 6, 7, 8))
        t |> equal(length(arr), 2)
        t |> success(arr[0] == short4(1, 2, 3, 4), "array read 0")
        t |> success(arr[1] == short4(5, 6, 7, 8), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = short4(1, 2, 3, 4)
        var data <- mem_archive_save(a)
        var back : short4
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = short4(1, 2, 3, 4)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
    t |> run("shift right") @(t : T?) {
        let a = short4(-19, 26, -33, 40)
        t |> success(a >> 2 == short4(-5, 6, -9, 10), "arithmetic shift")
        t |> success(a >> 18 == a >> 2, "count masks to lane width")
        t |> success(a >> 0 == a, "shift by zero")
    }
    t |> run("converts") @(t : T?) {
        let a = short4(1, 2, 3, 4)
        let w = int4(a)
        t |> success(w == int4(1, 2, 3, 4), "widen")
        t |> success(short4(w) == a, "narrow round trip")
        let s = short4_sat(int4(100000, -100000, 2, 3))
        t |> success(s == short4(32767, -32768, 2, 3), "sat clamp")
    }
}
