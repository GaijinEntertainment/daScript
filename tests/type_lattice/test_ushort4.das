// GENERATED by utils/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/dasgen/gen_type_conformance.das
options gen2
require dastest/testing_boost public
require daslib/archive

struct Wrap_ushort4 {
    pad : uint8
    v : ushort4
}

def roundtrip_ushort4(v : ushort4) : ushort4 => v

[test]
def test_ushort4_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<ushort4>), 8)
        t |> equal(typeinfo alignof(type<ushort4>), 2)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : ushort4
        t |> success(z == ushort4(0u, 0u, 0u, 0u), "zero init")
        t |> success(default<ushort4> == ushort4(0u, 0u, 0u, 0u), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = ushort4(1u, 2u, 3u, 4u)
        let b = ushort4(1u, 2u, 3u, 4u)
        let c = ushort4(2u, 3u, 4u, 5u)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
        t |> success(ushort4(3u) == ushort4(3u, 3u, 3u, 3u), "splat ctor")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = ushort4(1u, 2u, 3u, 4u)
        var c = a
        c = ushort4(5u, 6u, 7u, 8u)
        t |> success(a == ushort4(1u, 2u, 3u, 4u), "source unchanged")
        t |> success(c == ushort4(5u, 6u, 7u, 8u), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = ushort4(1u, 2u, 3u, 4u)
        t |> success(roundtrip_ushort4(a) == a, "roundtrip")
    }
    t |> run("index read and write") @(t : T?) {
        var a = ushort4(1u, 2u, 3u, 4u)
        t |> equal(a[0], uint16(1))
        t |> equal(a[1], uint16(2))
        t |> equal(a[2], uint16(3))
        t |> equal(a[3], uint16(4))
        a[3] = uint16(9)
        t |> equal(a[3], uint16(9))
    }
    t |> run("swizzle") @(t : T?) {
        var a = ushort4(1u, 2u, 3u, 4u)
        t |> equal(a.x, uint16(1))
        t |> equal(a.w, uint16(4))
        t |> success(a.wzyx == ushort4(4u, 3u, 2u, 1u), "reverse read")
        t |> success(a.xy == ushort2(1u, 2u), "pair read")
        a.w = uint16(9)
        t |> equal(a.w, uint16(9))
        a.xy = ushort2(7u, 8u)
        t |> equal(a.x, uint16(7))
        t |> equal(a.y, uint16(8))
    }
    t |> run("s-namespace swizzle") @(t : T?) {
        var a = ushort4(1u, 2u, 3u, 4u)
        t |> equal(a.s3, uint16(4))
        t |> success(a.s30 == ushort2(4u, 1u), "pair reorder")
        a.s0 = uint16(9)
        t |> equal(a.s0, uint16(9))
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_ushort4(pad = uint8(1), v = ushort4(1u, 2u, 3u, 4u))
        t |> success(w.v == ushort4(1u, 2u, 3u, 4u), "field read")
        w.v = ushort4(5u, 6u, 7u, 8u)
        t |> success(w.v == ushort4(5u, 6u, 7u, 8u), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_ushort4>), 10)
    }
    t |> run("array element") @(t : T?) {
        var arr : array<ushort4>
        arr |> push(ushort4(1u, 2u, 3u, 4u))
        arr |> push(ushort4(5u, 6u, 7u, 8u))
        t |> equal(length(arr), 2)
        t |> success(arr[0] == ushort4(1u, 2u, 3u, 4u), "array read 0")
        t |> success(arr[1] == ushort4(5u, 6u, 7u, 8u), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = ushort4(1u, 2u, 3u, 4u)
        var data <- mem_archive_save(a)
        var back : ushort4
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = ushort4(1u, 2u, 3u, 4u)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
    t |> run("shift right") @(t : T?) {
        let a = ushort4(200u, 203u, 206u, 209u)
        t |> success(a >> 2 == ushort4(50u, 50u, 51u, 52u), "logical shift")
        t |> success(a >> 18 == a >> 2, "count masks to lane width")
        t |> success(a >> 0 == a, "shift by zero")
    }
    t |> run("converts") @(t : T?) {
        let a = ushort4(1u, 2u, 3u, 4u)
        let w = uint4(a)
        t |> success(w == uint4(1u, 2u, 3u, 4u), "widen")
        t |> success(ushort4(w) == a, "narrow round trip")
        let s = ushort4_sat(uint4(100000u, 1u, 2u, 3u))
        t |> success(s == ushort4(65535u, 1u, 2u, 3u), "sat clamp")
    }
}
