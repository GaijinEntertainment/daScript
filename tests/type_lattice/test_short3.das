// GENERATED by utils/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/dasgen/gen_type_conformance.das
options gen2
require dastest/testing_boost public
require daslib/archive

struct Wrap_short3 {
    pad : uint8
    v : short3
}

def roundtrip_short3(v : short3) : short3 => v

[test]
def test_short3_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<short3>), 6)
        t |> equal(typeinfo alignof(type<short3>), 2)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : short3
        t |> success(z == short3(0, 0, 0), "zero init")
        t |> success(default<short3> == short3(0, 0, 0), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = short3(1, 2, 3)
        let b = short3(1, 2, 3)
        let c = short3(2, 3, 4)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
        t |> success(short3(3) == short3(3, 3, 3), "splat ctor")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = short3(1, 2, 3)
        var c = a
        c = short3(5, 6, 7)
        t |> success(a == short3(1, 2, 3), "source unchanged")
        t |> success(c == short3(5, 6, 7), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = short3(1, 2, 3)
        t |> success(roundtrip_short3(a) == a, "roundtrip")
    }
    t |> run("index read and write") @(t : T?) {
        var a = short3(1, 2, 3)
        t |> equal(a[0], int16(1))
        t |> equal(a[1], int16(2))
        t |> equal(a[2], int16(3))
        a[2] = int16(9)
        t |> equal(a[2], int16(9))
    }
    t |> run("swizzle") @(t : T?) {
        var a = short3(1, 2, 3)
        t |> equal(a.x, int16(1))
        t |> equal(a.z, int16(3))
        t |> success(a.zyx == short3(3, 2, 1), "reverse read")
        t |> success(a.xy == short2(1, 2), "pair read")
        a.z = int16(9)
        t |> equal(a.z, int16(9))
        a.xy = short2(7, 8)
        t |> equal(a.x, int16(7))
        t |> equal(a.y, int16(8))
    }
    t |> run("s-namespace swizzle") @(t : T?) {
        var a = short3(1, 2, 3)
        t |> equal(a.s2, int16(3))
        t |> success(a.s20 == short2(3, 1), "pair reorder")
        a.s0 = int16(9)
        t |> equal(a.s0, int16(9))
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_short3(pad = uint8(1), v = short3(1, 2, 3))
        t |> success(w.v == short3(1, 2, 3), "field read")
        w.v = short3(5, 6, 7)
        t |> success(w.v == short3(5, 6, 7), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_short3>), 8)
    }
    t |> run("array element") @(t : T?) {
        var arr : array<short3>
        arr |> push(short3(1, 2, 3))
        arr |> push(short3(5, 6, 7))
        t |> equal(length(arr), 2)
        t |> success(arr[0] == short3(1, 2, 3), "array read 0")
        t |> success(arr[1] == short3(5, 6, 7), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = short3(1, 2, 3)
        var data <- mem_archive_save(a)
        var back : short3
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = short3(1, 2, 3)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
    t |> run("shift right") @(t : T?) {
        let a = short3(-19, 26, -33)
        t |> success(a >> 2 == short3(-5, 6, -9), "arithmetic shift")
        t |> success(a >> 18 == a >> 2, "count masks to lane width")
        t |> success(a >> 0 == a, "shift by zero")
    }
    t |> run("converts") @(t : T?) {
        let a = short3(1, 2, 3)
        let w = int3(a)
        t |> success(w == int3(1, 2, 3), "widen")
        t |> success(short3(w) == a, "narrow round trip")
        let s = short3_sat(int3(100000, -100000, 2))
        t |> success(s == short3(32767, -32768, 2), "sat clamp")
    }
}
