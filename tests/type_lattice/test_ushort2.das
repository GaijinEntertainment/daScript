// GENERATED by utils/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/dasgen/gen_type_conformance.das
options gen2
require dastest/testing_boost public
require daslib/archive

struct Wrap_ushort2 {
    pad : uint8
    v : ushort2
}

def roundtrip_ushort2(v : ushort2) : ushort2 => v

[test]
def test_ushort2_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<ushort2>), 4)
        t |> equal(typeinfo alignof(type<ushort2>), 2)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : ushort2
        t |> success(z == ushort2(0u, 0u), "zero init")
        t |> success(default<ushort2> == ushort2(0u, 0u), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = ushort2(1u, 2u)
        let b = ushort2(1u, 2u)
        let c = ushort2(2u, 3u)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
        t |> success(ushort2(3u) == ushort2(3u, 3u), "splat ctor")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = ushort2(1u, 2u)
        var c = a
        c = ushort2(5u, 6u)
        t |> success(a == ushort2(1u, 2u), "source unchanged")
        t |> success(c == ushort2(5u, 6u), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = ushort2(1u, 2u)
        t |> success(roundtrip_ushort2(a) == a, "roundtrip")
    }
    t |> run("index read and write") @(t : T?) {
        var a = ushort2(1u, 2u)
        t |> equal(a[0], uint16(1))
        t |> equal(a[1], uint16(2))
        a[1] = uint16(9)
        t |> equal(a[1], uint16(9))
    }
    t |> run("swizzle") @(t : T?) {
        var a = ushort2(1u, 2u)
        t |> equal(a.x, uint16(1))
        t |> equal(a.y, uint16(2))
        t |> success(a.yx == ushort2(2u, 1u), "reverse read")
        a.y = uint16(9)
        t |> equal(a.y, uint16(9))
        a.xy = ushort2(7u, 8u)
        t |> equal(a.x, uint16(7))
        t |> equal(a.y, uint16(8))
    }
    t |> run("s-namespace swizzle") @(t : T?) {
        var a = ushort2(1u, 2u)
        t |> equal(a.s1, uint16(2))
        t |> success(a.s10 == ushort2(2u, 1u), "pair reorder")
        a.s0 = uint16(9)
        t |> equal(a.s0, uint16(9))
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_ushort2(pad = uint8(1), v = ushort2(1u, 2u))
        t |> success(w.v == ushort2(1u, 2u), "field read")
        w.v = ushort2(5u, 6u)
        t |> success(w.v == ushort2(5u, 6u), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_ushort2>), 6)
    }
    t |> run("array element") @(t : T?) {
        var arr : array<ushort2>
        arr |> push(ushort2(1u, 2u))
        arr |> push(ushort2(5u, 6u))
        t |> equal(length(arr), 2)
        t |> success(arr[0] == ushort2(1u, 2u), "array read 0")
        t |> success(arr[1] == ushort2(5u, 6u), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = ushort2(1u, 2u)
        var data <- mem_archive_save(a)
        var back : ushort2
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = ushort2(1u, 2u)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
    t |> run("shift right") @(t : T?) {
        let a = ushort2(200u, 203u)
        t |> success(a >> 2 == ushort2(50u, 50u), "logical shift")
        t |> success(a >> 18 == a >> 2, "count masks to lane width")
        t |> success(a >> 0 == a, "shift by zero")
    }
    t |> run("converts") @(t : T?) {
        let a = ushort2(1u, 2u)
        let w = uint2(a)
        t |> success(w == uint2(1u, 2u), "widen")
        t |> success(ushort2(w) == a, "narrow round trip")
        let s = ushort2_sat(uint2(100000u, 1u))
        t |> success(s == ushort2(65535u, 1u), "sat clamp")
    }
}
