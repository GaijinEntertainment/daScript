// GENERATED by utils/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/dasgen/gen_type_conformance.das
options gen2
options _function_length = 0
require dastest/testing_boost public
require daslib/archive

struct Wrap_half3 {
    pad : uint8
    v : half3
}

def roundtrip_half3(v : half3) : half3 => v

[test]
def test_half3_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<half3>), 6)
        t |> equal(typeinfo alignof(type<half3>), 2)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : half3
        t |> success(z == half3(0., 0., 0.), "zero init")
        t |> success(default<half3> == half3(0., 0., 0.), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = half3(1., 2., 3.)
        let b = half3(1., 2., 3.)
        let c = half3(2., 3., 4.)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
        t |> success(half3(3.) == half3(3., 3., 3.), "splat ctor")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = half3(1., 2., 3.)
        var c = a
        c = half3(5., 6., 7.)
        t |> success(a == half3(1., 2., 3.), "source unchanged")
        t |> success(c == half3(5., 6., 7.), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = half3(1., 2., 3.)
        t |> success(roundtrip_half3(a) == a, "roundtrip")
    }
    t |> run("index read and write") @(t : T?) {
        var a = half3(1., 2., 3.)
        t |> equal(a[0], 1h)
        t |> equal(a[1], 2h)
        t |> equal(a[2], 3h)
        a[2] = 9h
        t |> equal(a[2], 9h)
    }
    t |> run("swizzle") @(t : T?) {
        var a = half3(1., 2., 3.)
        t |> equal(a.x, 1h)
        t |> equal(a.z, 3h)
        t |> success(a.zyx == half3(3., 2., 1.), "reverse read")
        t |> success(a.xy == half2(1., 2.), "pair read")
        a.z = 9h
        t |> equal(a.z, 9h)
        a.xy = half2(7., 8.)
        t |> equal(a.x, 7h)
        t |> equal(a.y, 8h)
    }
    t |> run("s-namespace swizzle") @(t : T?) {
        var a = half3(1., 2., 3.)
        t |> equal(a.s2, 3h)
        t |> success(a.s20 == half2(3., 1.), "pair reorder")
        a.s0 = 9h
        t |> equal(a.s0, 9h)
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_half3(pad = uint8(1), v = half3(1., 2., 3.))
        t |> success(w.v == half3(1., 2., 3.), "field read")
        w.v = half3(5., 6., 7.)
        t |> success(w.v == half3(5., 6., 7.), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_half3>), 8)
    }
    t |> run("array element") @(t : T?) {
        var arr <- [half3(1., 2., 3.), half3(5., 6., 7.)]
        t |> equal(length(arr), 2)
        t |> success(arr[0] == half3(1., 2., 3.), "array read 0")
        t |> success(arr[1] == half3(5., 6., 7.), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = half3(1., 2., 3.)
        var data <- mem_archive_save(a)
        var back : half3
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = half3(1., 2., 3.)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
    t |> run("arithmetic") @(t : T?) {
        let a = half3(1., 2., 3.)
        let b = half3(2., 4., 6.)
        t |> success(a + b == half3(3., 6., 9.), "add")
        t |> success(b - a == half3(1., 2., 3.), "sub")
        t |> success(a * b == half3(2., 8., 18.), "mul")
        t |> success(b / a == half3(2., 2., 2.), "div")
        var c = a
        c += b
        t |> success(c == a + b, "add assign")
        c -= b
        t |> success(c == a, "sub assign")
        t |> success(-a == half3(-1., -2., -3.), "unary minus")
        t |> success(a * 2h == a + a, "vec mul scalar")
        t |> success(2h * a == a + a, "scalar mul vec")
        t |> success((a + a) / 2h == a, "vec div scalar")
        c = a
        c *= 2h
        t |> success(c == a + a, "mul assign scalar")
    }
    t |> run("converts") @(t : T?) {
        let a = half3(1., 2., 3.)
        let f = float3(a)
        t |> success(f == float3(1., 2., 3.), "widen to float")
        t |> success(half3(f) == a, "narrow round trip")
    }
}
