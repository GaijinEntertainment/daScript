// GENERATED by utils/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/dasgen/gen_type_conformance.das
options gen2
options _function_length = 0
require dastest/testing_boost public
require daslib/archive

struct Wrap_byte3 {
    pad : uint8
    v : byte3
}

def roundtrip_byte3(v : byte3) : byte3 => v

[test]
def test_byte3_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<byte3>), 3)
        t |> equal(typeinfo alignof(type<byte3>), 1)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : byte3
        t |> success(z == byte3(0, 0, 0), "zero init")
        t |> success(default<byte3> == byte3(0, 0, 0), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = byte3(1, 2, 3)
        let b = byte3(1, 2, 3)
        let c = byte3(2, 3, 4)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
        t |> success(byte3(3) == byte3(3, 3, 3), "splat ctor")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = byte3(1, 2, 3)
        var c = a
        c = byte3(5, 6, 7)
        t |> success(a == byte3(1, 2, 3), "source unchanged")
        t |> success(c == byte3(5, 6, 7), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = byte3(1, 2, 3)
        t |> success(roundtrip_byte3(a) == a, "roundtrip")
    }
    t |> run("index read and write") @(t : T?) {
        var a = byte3(1, 2, 3)
        t |> equal(a[0], int8(1))
        t |> equal(a[1], int8(2))
        t |> equal(a[2], int8(3))
        a[2] = int8(9)
        t |> equal(a[2], int8(9))
    }
    t |> run("swizzle") @(t : T?) {
        var a = byte3(1, 2, 3)
        t |> equal(a.x, int8(1))
        t |> equal(a.z, int8(3))
        t |> success(a.zyx == byte3(3, 2, 1), "reverse read")
        t |> success(a.xy == byte2(1, 2), "pair read")
        a.z = int8(9)
        t |> equal(a.z, int8(9))
        a.xy = byte2(7, 8)
        t |> equal(a.x, int8(7))
        t |> equal(a.y, int8(8))
    }
    t |> run("s-namespace swizzle") @(t : T?) {
        var a = byte3(1, 2, 3)
        t |> equal(a.s2, int8(3))
        t |> success(a.s20 == byte2(3, 1), "pair reorder")
        a.s0 = int8(9)
        t |> equal(a.s0, int8(9))
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_byte3(pad = uint8(1), v = byte3(1, 2, 3))
        t |> success(w.v == byte3(1, 2, 3), "field read")
        w.v = byte3(5, 6, 7)
        t |> success(w.v == byte3(5, 6, 7), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_byte3>), 4)
    }
    t |> run("array element") @(t : T?) {
        var arr <- [byte3(1, 2, 3), byte3(5, 6, 7)]
        t |> equal(length(arr), 2)
        t |> success(arr[0] == byte3(1, 2, 3), "array read 0")
        t |> success(arr[1] == byte3(5, 6, 7), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = byte3(1, 2, 3)
        var data <- mem_archive_save(a)
        var back : byte3
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = byte3(1, 2, 3)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
    t |> run("shift right") @(t : T?) {
        let a = byte3(-19, 26, -33)
        t |> success(a >> 2 == byte3(-5, 6, -9), "arithmetic shift")
        t |> success(a >> 10 == a >> 2, "count masks to lane width")
        t |> success(a >> 0 == a, "shift by zero")
        var c = a
        c >>= 2
        t |> success(c == a >> 2, "shift right assign")
    }
    t |> run("shift left") @(t : T?) {
        let a = byte3(-19, 26, -33)
        t |> success(a << 2 == byte3(-76, 104, 124), "lanes wrap")
        t |> success(a << 10 == a << 2, "count masks to lane width")
        var c = a
        c <<= 2
        t |> success(c == a << 2, "shift left assign")
    }
    t |> run("bitwise") @(t : T?) {
        let a = byte3(-19, 26, -33)
        let b = byte3(45, 50, 55)
        t |> success((a & b) == byte3(45, 18, 23), "and")
        t |> success((a | b) == byte3(-19, 58, -1), "or")
        t |> success((a ^ b) == byte3(-64, 40, -24), "xor")
        var c = a
        c &= b
        t |> success(c == (a & b), "and assign")
        c = a
        c |= b
        t |> success(c == (a | b), "or assign")
        c = a
        c ^= b
        t |> success(c == (a ^ b), "xor assign")
    }
    t |> run("converts") @(t : T?) {
        let a = byte3(1, 2, 3)
        let w = int3(a)
        t |> success(w == int3(1, 2, 3), "widen")
        t |> success(byte3(w) == a, "narrow round trip")
        let s = byte3_sat(int3(100000, -100000, 2))
        t |> success(s == byte3(127, -128, 2), "sat clamp")
    }
}
