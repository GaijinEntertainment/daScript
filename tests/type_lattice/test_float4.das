// GENERATED by utils/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/dasgen/gen_type_conformance.das
options gen2
options _function_length = 0
require dastest/testing_boost public
require daslib/archive

struct Wrap_float4 {
    pad : uint8
    v : float4
}

def roundtrip_float4(v : float4) : float4 => v

[test]
def test_float4_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<float4>), 16)
        t |> equal(typeinfo alignof(type<float4>), 4)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : float4
        t |> success(z == float4(0., 0., 0., 0.), "zero init")
        t |> success(default<float4> == float4(0., 0., 0., 0.), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = float4(1., 2., 3., 4.)
        let b = float4(1., 2., 3., 4.)
        let c = float4(2., 3., 4., 5.)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
        t |> success(float4(3.) == float4(3., 3., 3., 3.), "splat ctor")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = float4(1., 2., 3., 4.)
        var c = a
        c = float4(5., 6., 7., 8.)
        t |> success(a == float4(1., 2., 3., 4.), "source unchanged")
        t |> success(c == float4(5., 6., 7., 8.), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = float4(1., 2., 3., 4.)
        t |> success(roundtrip_float4(a) == a, "roundtrip")
    }
    t |> run("index read and write") @(t : T?) {
        var a = float4(1., 2., 3., 4.)
        t |> equal(a[0], 1.)
        t |> equal(a[1], 2.)
        t |> equal(a[2], 3.)
        t |> equal(a[3], 4.)
        a[3] = 9.
        t |> equal(a[3], 9.)
    }
    t |> run("swizzle") @(t : T?) {
        var a = float4(1., 2., 3., 4.)
        t |> equal(a.x, 1.)
        t |> equal(a.w, 4.)
        t |> success(a.wzyx == float4(4., 3., 2., 1.), "reverse read")
        t |> success(a.xy == float2(1., 2.), "pair read")
        a.w = 9.
        t |> equal(a.w, 9.)
        a.xy = float2(7., 8.)
        t |> equal(a.x, 7.)
        t |> equal(a.y, 8.)
    }
    t |> run("s-namespace swizzle") @(t : T?) {
        var a = float4(1., 2., 3., 4.)
        t |> equal(a.s3, 4.)
        t |> success(a.s30 == float2(4., 1.), "pair reorder")
        a.s0 = 9.
        t |> equal(a.s0, 9.)
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_float4(pad = uint8(1), v = float4(1., 2., 3., 4.))
        t |> success(w.v == float4(1., 2., 3., 4.), "field read")
        w.v = float4(5., 6., 7., 8.)
        t |> success(w.v == float4(5., 6., 7., 8.), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_float4>), 20)
    }
    t |> run("array element") @(t : T?) {
        var arr <- [float4(1., 2., 3., 4.), float4(5., 6., 7., 8.)]
        t |> equal(length(arr), 2)
        t |> success(arr[0] == float4(1., 2., 3., 4.), "array read 0")
        t |> success(arr[1] == float4(5., 6., 7., 8.), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = float4(1., 2., 3., 4.)
        var data <- mem_archive_save(a)
        var back : float4
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = float4(1., 2., 3., 4.)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
    t |> run("arithmetic") @(t : T?) {
        let a = float4(1., 2., 3., 4.)
        let b = float4(2., 4., 6., 8.)
        t |> success(a + b == float4(3., 6., 9., 12.), "add")
        t |> success(b - a == float4(1., 2., 3., 4.), "sub")
        t |> success(a * b == float4(2., 8., 18., 32.), "mul")
        t |> success(b / a == float4(2., 2., 2., 2.), "div")
        var c = a
        c += b
        t |> success(c == a + b, "add assign")
        c -= b
        t |> success(c == a, "sub assign")
        t |> success(-a == float4(-1., -2., -3., -4.), "unary minus")
        t |> success(a * 2. == a + a, "vec mul scalar")
        t |> success(2. * a == a + a, "scalar mul vec")
        t |> success((a + a) / 2. == a, "vec div scalar")
        c = a
        c *= 2.
        t |> success(c == a + a, "mul assign scalar")
    }
}
