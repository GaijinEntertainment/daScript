// GENERATED by utils/internal/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/internal/dasgen/gen_type_conformance.das
options gen2
options _function_length = 0
require dastest/testing_boost public
require daslib/archive

struct Wrap_float3 {
    pad : uint8
    v : float3
}

def roundtrip_float3(v : float3) : float3 => v

[test]
def test_float3_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<float3>), 12)
        t |> equal(typeinfo alignof(type<float3>), 4)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : float3
        t |> success(z == float3(0., 0., 0.), "zero init")
        t |> success(default<float3> == float3(0., 0., 0.), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = float3(1., 2., 3.)
        let b = float3(1., 2., 3.)
        let c = float3(2., 3., 4.)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
        t |> success(float3(3.) == float3(3., 3., 3.), "splat ctor")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = float3(1., 2., 3.)
        var c = a
        c = float3(5., 6., 7.)
        t |> success(a == float3(1., 2., 3.), "source unchanged")
        t |> success(c == float3(5., 6., 7.), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = float3(1., 2., 3.)
        t |> success(roundtrip_float3(a) == a, "roundtrip")
    }
    t |> run("index read and write") @(t : T?) {
        var a = float3(1., 2., 3.)
        t |> equal(a[0], 1.)
        t |> equal(a[1], 2.)
        t |> equal(a[2], 3.)
        a[2] = 9.
        t |> equal(a[2], 9.)
    }
    t |> run("swizzle") @(t : T?) {
        var a = float3(1., 2., 3.)
        t |> equal(a.x, 1.)
        t |> equal(a.z, 3.)
        t |> success(a.zyx == float3(3., 2., 1.), "reverse read")
        t |> success(a.xy == float2(1., 2.), "pair read")
        a.z = 9.
        t |> equal(a.z, 9.)
        a.xy = float2(7., 8.)
        t |> equal(a.x, 7.)
        t |> equal(a.y, 8.)
    }
    t |> run("s-namespace swizzle") @(t : T?) {
        var a = float3(1., 2., 3.)
        t |> equal(a.s2, 3.)
        t |> success(a.s20 == float2(3., 1.), "pair reorder")
        a.s0 = 9.
        t |> equal(a.s0, 9.)
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_float3(pad = uint8(1), v = float3(1., 2., 3.))
        t |> success(w.v == float3(1., 2., 3.), "field read")
        w.v = float3(5., 6., 7.)
        t |> success(w.v == float3(5., 6., 7.), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_float3>), 16)
    }
    t |> run("array element") @(t : T?) {
        var arr <- [float3(1., 2., 3.), float3(5., 6., 7.)]
        t |> equal(length(arr), 2)
        t |> success(arr[0] == float3(1., 2., 3.), "array read 0")
        t |> success(arr[1] == float3(5., 6., 7.), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = float3(1., 2., 3.)
        var data <- mem_archive_save(a)
        var back : float3
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = float3(1., 2., 3.)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
    t |> run("arithmetic") @(t : T?) {
        let a = float3(1., 2., 3.)
        let b = float3(2., 4., 6.)
        t |> success(a + b == float3(3., 6., 9.), "add")
        t |> success(b - a == float3(1., 2., 3.), "sub")
        t |> success(a * b == float3(2., 8., 18.), "mul")
        t |> success(b / a == float3(2., 2., 2.), "div")
        var c = a
        c += b
        t |> success(c == a + b, "add assign")
        c -= b
        t |> success(c == a, "sub assign")
        t |> success(-a == float3(-1., -2., -3.), "unary minus")
        t |> success(a * 2. == a + a, "vec mul scalar")
        t |> success(2. * a == a + a, "scalar mul vec")
        t |> success((a + a) / 2. == a, "vec div scalar")
        c = a
        c *= 2.
        t |> success(c == a + a, "mul assign scalar")
    }
}
