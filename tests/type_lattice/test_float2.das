// GENERATED by utils/internal/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/internal/dasgen/gen_type_conformance.das
options gen2
options _function_length = 0
require dastest/testing_boost public
require daslib/archive

struct Wrap_float2 {
    pad : uint8
    v : float2
}

def roundtrip_float2(v : float2) : float2 => v

[test]
def test_float2_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<float2>), 8)
        t |> equal(typeinfo alignof(type<float2>), 4)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : float2
        t |> success(z == float2(0., 0.), "zero init")
        t |> success(default<float2> == float2(0., 0.), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = float2(1., 2.)
        let b = float2(1., 2.)
        let c = float2(2., 3.)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
        t |> success(float2(3.) == float2(3., 3.), "splat ctor")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = float2(1., 2.)
        var c = a
        c = float2(5., 6.)
        t |> success(a == float2(1., 2.), "source unchanged")
        t |> success(c == float2(5., 6.), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = float2(1., 2.)
        t |> success(roundtrip_float2(a) == a, "roundtrip")
    }
    t |> run("index read and write") @(t : T?) {
        var a = float2(1., 2.)
        t |> equal(a[0], 1.)
        t |> equal(a[1], 2.)
        a[1] = 9.
        t |> equal(a[1], 9.)
    }
    t |> run("swizzle") @(t : T?) {
        var a = float2(1., 2.)
        t |> equal(a.x, 1.)
        t |> equal(a.y, 2.)
        t |> success(a.yx == float2(2., 1.), "reverse read")
        a.y = 9.
        t |> equal(a.y, 9.)
        a.xy = float2(7., 8.)
        t |> equal(a.x, 7.)
        t |> equal(a.y, 8.)
    }
    t |> run("s-namespace swizzle") @(t : T?) {
        var a = float2(1., 2.)
        t |> equal(a.s1, 2.)
        t |> success(a.s10 == float2(2., 1.), "pair reorder")
        a.s0 = 9.
        t |> equal(a.s0, 9.)
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_float2(pad = uint8(1), v = float2(1., 2.))
        t |> success(w.v == float2(1., 2.), "field read")
        w.v = float2(5., 6.)
        t |> success(w.v == float2(5., 6.), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_float2>), 12)
    }
    t |> run("array element") @(t : T?) {
        var arr <- [float2(1., 2.), float2(5., 6.)]
        t |> equal(length(arr), 2)
        t |> success(arr[0] == float2(1., 2.), "array read 0")
        t |> success(arr[1] == float2(5., 6.), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = float2(1., 2.)
        var data <- mem_archive_save(a)
        var back : float2
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = float2(1., 2.)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
    t |> run("arithmetic") @(t : T?) {
        let a = float2(1., 2.)
        let b = float2(2., 4.)
        t |> success(a + b == float2(3., 6.), "add")
        t |> success(b - a == float2(1., 2.), "sub")
        t |> success(a * b == float2(2., 8.), "mul")
        t |> success(b / a == float2(2., 2.), "div")
        var c = a
        c += b
        t |> success(c == a + b, "add assign")
        c -= b
        t |> success(c == a, "sub assign")
        t |> success(-a == float2(-1., -2.), "unary minus")
        t |> success(a * 2. == a + a, "vec mul scalar")
        t |> success(2. * a == a + a, "scalar mul vec")
        t |> success((a + a) / 2. == a, "vec div scalar")
        c = a
        c *= 2.
        t |> success(c == a + a, "mul assign scalar")
    }
}
