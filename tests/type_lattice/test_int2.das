// GENERATED by utils/internal/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/internal/dasgen/gen_type_conformance.das
options gen2
options _function_length = 0
require dastest/testing_boost public
require daslib/archive

struct Wrap_int2 {
    pad : uint8
    v : int2
}

def roundtrip_int2(v : int2) : int2 => v

[test]
def test_int2_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<int2>), 8)
        t |> equal(typeinfo alignof(type<int2>), 4)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : int2
        t |> success(z == int2(0, 0), "zero init")
        t |> success(default<int2> == int2(0, 0), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = int2(1, 2)
        let b = int2(1, 2)
        let c = int2(2, 3)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
        t |> success(int2(3) == int2(3, 3), "splat ctor")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = int2(1, 2)
        var c = a
        c = int2(5, 6)
        t |> success(a == int2(1, 2), "source unchanged")
        t |> success(c == int2(5, 6), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = int2(1, 2)
        t |> success(roundtrip_int2(a) == a, "roundtrip")
    }
    t |> run("index read and write") @(t : T?) {
        var a = int2(1, 2)
        t |> equal(a[0], 1)
        t |> equal(a[1], 2)
        a[1] = 9
        t |> equal(a[1], 9)
    }
    t |> run("swizzle") @(t : T?) {
        var a = int2(1, 2)
        t |> equal(a.x, 1)
        t |> equal(a.y, 2)
        t |> success(a.yx == int2(2, 1), "reverse read")
        a.y = 9
        t |> equal(a.y, 9)
        a.xy = int2(7, 8)
        t |> equal(a.x, 7)
        t |> equal(a.y, 8)
    }
    t |> run("s-namespace swizzle") @(t : T?) {
        var a = int2(1, 2)
        t |> equal(a.s1, 2)
        t |> success(a.s10 == int2(2, 1), "pair reorder")
        a.s0 = 9
        t |> equal(a.s0, 9)
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_int2(pad = uint8(1), v = int2(1, 2))
        t |> success(w.v == int2(1, 2), "field read")
        w.v = int2(5, 6)
        t |> success(w.v == int2(5, 6), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_int2>), 12)
    }
    t |> run("array element") @(t : T?) {
        var arr <- [int2(1, 2), int2(5, 6)]
        t |> equal(length(arr), 2)
        t |> success(arr[0] == int2(1, 2), "array read 0")
        t |> success(arr[1] == int2(5, 6), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = int2(1, 2)
        var data <- mem_archive_save(a)
        var back : int2
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = int2(1, 2)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
    t |> run("arithmetic") @(t : T?) {
        let a = int2(1, 2)
        let b = int2(2, 4)
        t |> success(a + b == int2(3, 6), "add")
        t |> success(b - a == int2(1, 2), "sub")
        t |> success(a * b == int2(2, 8), "mul")
        var c = a
        c += b
        t |> success(c == a + b, "add assign")
        c -= b
        t |> success(c == a, "sub assign")
        t |> success(-a == int2(-1, -2), "unary minus")
        t |> success(a * 2 == a + a, "vec mul scalar")
        t |> success(2 * a == a + a, "scalar mul vec")
        c = a
        c *= 2
        t |> success(c == a + a, "mul assign scalar")
    }
    t |> run("shift right") @(t : T?) {
        let a = int2(-19, 26)
        t |> success(a >> 2 == int2(-5, 6), "arithmetic shift")
        t |> success(a >> 34 == a >> 2, "count masks to lane width")
        t |> success(a >> 0 == a, "shift by zero")
        var c = a
        c >>= 2
        t |> success(c == a >> 2, "shift right assign")
    }
    t |> run("shift left") @(t : T?) {
        let a = int2(-19, 26)
        t |> success(a << 2 == int2(-76, 104), "lanes wrap")
        t |> success(a << 34 == a << 2, "count masks to lane width")
        var c = a
        c <<= 2
        t |> success(c == a << 2, "shift left assign")
    }
    t |> run("bitwise") @(t : T?) {
        let a = int2(-19, 26)
        let b = int2(45, 50)
        t |> success((a & b) == int2(45, 18), "and")
        t |> success((a | b) == int2(-19, 58), "or")
        t |> success((a ^ b) == int2(-64, 40), "xor")
        var c = a
        c &= b
        t |> success(c == (a & b), "and assign")
        c = a
        c |= b
        t |> success(c == (a | b), "or assign")
        c = a
        c ^= b
        t |> success(c == (a ^ b), "xor assign")
    }
}
