// GENERATED by utils/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/dasgen/gen_type_conformance.das
options gen2
options _function_length = 0
require dastest/testing_boost public
require daslib/archive

struct Wrap_byte2 {
    pad : uint8
    v : byte2
}

def roundtrip_byte2(v : byte2) : byte2 => v

[test]
def test_byte2_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<byte2>), 2)
        t |> equal(typeinfo alignof(type<byte2>), 1)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : byte2
        t |> success(z == byte2(0, 0), "zero init")
        t |> success(default<byte2> == byte2(0, 0), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = byte2(1, 2)
        let b = byte2(1, 2)
        let c = byte2(2, 3)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
        t |> success(byte2(3) == byte2(3, 3), "splat ctor")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = byte2(1, 2)
        var c = a
        c = byte2(5, 6)
        t |> success(a == byte2(1, 2), "source unchanged")
        t |> success(c == byte2(5, 6), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = byte2(1, 2)
        t |> success(roundtrip_byte2(a) == a, "roundtrip")
    }
    t |> run("index read and write") @(t : T?) {
        var a = byte2(1, 2)
        t |> equal(a[0], int8(1))
        t |> equal(a[1], int8(2))
        a[1] = int8(9)
        t |> equal(a[1], int8(9))
    }
    t |> run("swizzle") @(t : T?) {
        var a = byte2(1, 2)
        t |> equal(a.x, int8(1))
        t |> equal(a.y, int8(2))
        t |> success(a.yx == byte2(2, 1), "reverse read")
        a.y = int8(9)
        t |> equal(a.y, int8(9))
        a.xy = byte2(7, 8)
        t |> equal(a.x, int8(7))
        t |> equal(a.y, int8(8))
    }
    t |> run("s-namespace swizzle") @(t : T?) {
        var a = byte2(1, 2)
        t |> equal(a.s1, int8(2))
        t |> success(a.s10 == byte2(2, 1), "pair reorder")
        a.s0 = int8(9)
        t |> equal(a.s0, int8(9))
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_byte2(pad = uint8(1), v = byte2(1, 2))
        t |> success(w.v == byte2(1, 2), "field read")
        w.v = byte2(5, 6)
        t |> success(w.v == byte2(5, 6), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_byte2>), 3)
    }
    t |> run("array element") @(t : T?) {
        var arr <- [byte2(1, 2), byte2(5, 6)]
        t |> equal(length(arr), 2)
        t |> success(arr[0] == byte2(1, 2), "array read 0")
        t |> success(arr[1] == byte2(5, 6), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = byte2(1, 2)
        var data <- mem_archive_save(a)
        var back : byte2
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = byte2(1, 2)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
    t |> run("shift right") @(t : T?) {
        let a = byte2(-19, 26)
        t |> success(a >> 2 == byte2(-5, 6), "arithmetic shift")
        t |> success(a >> 10 == a >> 2, "count masks to lane width")
        t |> success(a >> 0 == a, "shift by zero")
        var c = a
        c >>= 2
        t |> success(c == a >> 2, "shift right assign")
    }
    t |> run("shift left") @(t : T?) {
        let a = byte2(-19, 26)
        t |> success(a << 2 == byte2(-76, 104), "lanes wrap")
        t |> success(a << 10 == a << 2, "count masks to lane width")
        var c = a
        c <<= 2
        t |> success(c == a << 2, "shift left assign")
    }
    t |> run("bitwise") @(t : T?) {
        let a = byte2(-19, 26)
        let b = byte2(45, 50)
        t |> success((a & b) == byte2(45, 18), "and")
        t |> success((a | b) == byte2(-19, 58), "or")
        t |> success((a ^ b) == byte2(-64, 40), "xor")
        var c = a
        c &= b
        t |> success(c == (a & b), "and assign")
        c = a
        c |= b
        t |> success(c == (a | b), "or assign")
        c = a
        c ^= b
        t |> success(c == (a ^ b), "xor assign")
    }
    t |> run("converts") @(t : T?) {
        let a = byte2(1, 2)
        let w = int2(a)
        t |> success(w == int2(1, 2), "widen")
        t |> success(byte2(w) == a, "narrow round trip")
        let s = byte2_sat(int2(100000, -100000))
        t |> success(s == byte2(127, -128), "sat clamp")
    }
}
