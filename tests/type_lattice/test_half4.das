// GENERATED by utils/dasgen/gen_type_conformance.das — DO NOT EDIT
// regen: daslang utils/dasgen/gen_type_conformance.das
options gen2
options _function_length = 0
require dastest/testing_boost public
require daslib/archive

struct Wrap_half4 {
    pad : uint8
    v : half4
}

def roundtrip_half4(v : half4) : half4 => v

[test]
def test_half4_conformance(t : T?) {
    t |> run("sizeof and alignof") @(t : T?) {
        t |> equal(typeinfo sizeof(type<half4>), 8)
        t |> equal(typeinfo alignof(type<half4>), 2)
    }
    t |> run("zero init and default") @(t : T?) {
        let z : half4
        t |> success(z == half4(0., 0., 0., 0.), "zero init")
        t |> success(default<half4> == half4(0., 0., 0., 0.), "default<>")
    }
    t |> run("ctor and equality") @(t : T?) {
        let a = half4(1., 2., 3., 4.)
        let b = half4(1., 2., 3., 4.)
        let c = half4(2., 3., 4., 5.)
        t |> success(a == b, "a == b")
        t |> success(a != c, "a != c")
        t |> success(half4(3.) == half4(3., 3., 3., 3.), "splat ctor")
    }
    t |> run("copy is value copy") @(t : T?) {
        let a = half4(1., 2., 3., 4.)
        var c = a
        c = half4(5., 6., 7., 8.)
        t |> success(a == half4(1., 2., 3., 4.), "source unchanged")
        t |> success(c == half4(5., 6., 7., 8.), "copy reassigned")
    }
    t |> run("pass and return by value") @(t : T?) {
        let a = half4(1., 2., 3., 4.)
        t |> success(roundtrip_half4(a) == a, "roundtrip")
    }
    t |> run("index read and write") @(t : T?) {
        var a = half4(1., 2., 3., 4.)
        t |> equal(a[0], 1h)
        t |> equal(a[1], 2h)
        t |> equal(a[2], 3h)
        t |> equal(a[3], 4h)
        a[3] = 9h
        t |> equal(a[3], 9h)
    }
    t |> run("swizzle") @(t : T?) {
        var a = half4(1., 2., 3., 4.)
        t |> equal(a.x, 1h)
        t |> equal(a.w, 4h)
        t |> success(a.wzyx == half4(4., 3., 2., 1.), "reverse read")
        t |> success(a.xy == half2(1., 2.), "pair read")
        a.w = 9h
        t |> equal(a.w, 9h)
        a.xy = half2(7., 8.)
        t |> equal(a.x, 7h)
        t |> equal(a.y, 8h)
    }
    t |> run("s-namespace swizzle") @(t : T?) {
        var a = half4(1., 2., 3., 4.)
        t |> equal(a.s3, 4h)
        t |> success(a.s30 == half2(4., 1.), "pair reorder")
        a.s0 = 9h
        t |> equal(a.s0, 9h)
    }
    t |> run("struct field and layout") @(t : T?) {
        var w = Wrap_half4(pad = uint8(1), v = half4(1., 2., 3., 4.))
        t |> success(w.v == half4(1., 2., 3., 4.), "field read")
        w.v = half4(5., 6., 7., 8.)
        t |> success(w.v == half4(5., 6., 7., 8.), "field write")
        t |> equal(typeinfo sizeof(type<Wrap_half4>), 10)
    }
    t |> run("array element") @(t : T?) {
        var arr <- [half4(1., 2., 3., 4.), half4(5., 6., 7., 8.)]
        t |> equal(length(arr), 2)
        t |> success(arr[0] == half4(1., 2., 3., 4.), "array read 0")
        t |> success(arr[1] == half4(5., 6., 7., 8.), "array read 1")
        delete arr
    }
    t |> run("archive round trip") @(t : T?) {
        var a = half4(1., 2., 3., 4.)
        var data <- mem_archive_save(a)
        var back : half4
        data |> mem_archive_load(back)
        delete data
        t |> success(back == a, "mem archive")
    }
    t |> run("print") @(t : T?) {
        let a = half4(1., 2., 3., 4.)
        let s = "{a}"
        t |> success(!empty(s), "interpolates")
    }
    t |> run("arithmetic") @(t : T?) {
        let a = half4(1., 2., 3., 4.)
        let b = half4(2., 4., 6., 8.)
        t |> success(a + b == half4(3., 6., 9., 12.), "add")
        t |> success(b - a == half4(1., 2., 3., 4.), "sub")
        t |> success(a * b == half4(2., 8., 18., 32.), "mul")
        t |> success(b / a == half4(2., 2., 2., 2.), "div")
        var c = a
        c += b
        t |> success(c == a + b, "add assign")
        c -= b
        t |> success(c == a, "sub assign")
        t |> success(-a == half4(-1., -2., -3., -4.), "unary minus")
        t |> success(a * 2h == a + a, "vec mul scalar")
        t |> success(2h * a == a + a, "scalar mul vec")
        t |> success((a + a) / 2h == a, "vec div scalar")
        c = a
        c *= 2h
        t |> success(c == a + a, "mul assign scalar")
    }
    t |> run("converts") @(t : T?) {
        let a = half4(1., 2., 3., 4.)
        let f = float4(a)
        t |> success(f == float4(1., 2., 3., 4.), "widen to float")
        t |> success(half4(f) == a, "narrow round trip")
    }
}
