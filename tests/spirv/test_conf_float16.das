// GENERATED by utils/internal/dasgen/gen_shader_conformance.das — DO NOT EDIT
// regen: daslang utils/internal/dasgen/gen_shader_conformance.das
options gen2
options indenting = 4

require dastest/testing_boost public
require _spirv_common
require spirv/spirv_shader
require spirv/spirv_dis
require spirv/spirv_grammar

var @ssbo @binding = 0 conf_float16_data : array<float>

[compute_shader(local_size_x=64, name="conf_float16_spv"), marker(no_coverage)]
def conf_float16_kernel {
    let gi = gl_GlobalInvocationID.x
    var v = 1.5h
    let s = float16(conf_float16_data[gi])
    v = v + s
    v = v * 2h
    v = v - s
    let r = v
    conf_float16_data[gi] = float(r)
}

[test]
def test_conf_float16_spirv(t : T?) {
    t |> run("conformance float16: emits and validates") <| @(t : T?) {
        var words <- clone_to_move(conf_float16_spv)
        t |> success(length(words) > 5, "has words")
        t |> equal(words[0], SPV_MAGIC)
        t |> equal(count_op(words, SpvOp.EntryPoint), 1)
        let r = validate_spirv(words)
        if (r.ran) {
            t |> success(r.ok, "spirv-val: {r.msg}")
        } else {
            feint("spirv-val not found locally; skipping (CI enforces)\n")
        }
        delete words
    }
}
