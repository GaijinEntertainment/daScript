// GENERATED by utils/internal/dasgen/gen_shader_conformance.das — DO NOT EDIT
// regen: daslang utils/internal/dasgen/gen_shader_conformance.das
options gen2
options indenting = 4

require dastest/testing_boost public
require _glsl_common
require glsl/glsl_opengl

var @inout conf_float2_uv : float2
var @out conf_float2_color : float4

[fragment_program(name="conf_float2_glsl")]
def conf_float2_fragment {
    var v = float2(1., 2.)
    let s = float2(conf_float2_uv.x)
    v = v + s
    v = v * float2(2., 2.)
    v = v - s
    let r = v.x + v.y
    conf_float2_color = float4(r, 0., 0., 1.)
}

[test]
def test_conf_float2_glsl(t : T?) {
    t |> run("conformance float2: emits and validates") <| @(t : T?) {
        t |> success(has(conf_float2_glsl, "vec2"), "spells vec2")
        let r = validate_glsl(conf_float2_glsl, "frag")
        if (r.ran) {
            t |> success(r.ok, "glslang: {r.msg}")
        } else {
            feint("glslangValidator not found locally; skipping (CI enforces)\n")
        }
    }
}
