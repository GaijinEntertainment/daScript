// GENERATED by utils/internal/dasgen/gen_shader_conformance.das — DO NOT EDIT
// regen: daslang utils/internal/dasgen/gen_shader_conformance.das
options gen2
options indenting = 4

require dastest/testing_boost public
require _glsl_common
require glsl/glsl_opengl

var @inout conf_int2_uv : float2
var @out conf_int2_color : float4

[fragment_program(name="conf_int2_glsl")]
def conf_int2_fragment {
    var v = int2(1, 2)
    let s = int2(int(conf_int2_uv.x))
    v = v + s
    v = v * int2(2, 2)
    v = v - s
    let r = v.x + v.y
    conf_int2_color = float4(float(r), 0., 0., 1.)
}

[test]
def test_conf_int2_glsl(t : T?) {
    t |> run("conformance int2: emits and validates") <| @(t : T?) {
        t |> success(has(conf_int2_glsl, "ivec2"), "spells ivec2")
        let r = validate_glsl(conf_int2_glsl, "frag")
        if (r.ran) {
            t |> success(r.ok, "glslang: {r.msg}")
        } else {
            feint("glslangValidator not found locally; skipping (CI enforces)\n")
        }
    }
}
