// GENERATED by utils/internal/dasgen/gen_shader_conformance.das — DO NOT EDIT
// regen: daslang utils/internal/dasgen/gen_shader_conformance.das
options gen2
options indenting = 4

require dastest/testing_boost public
require _glsl_common
require glsl/glsl_opengl

var @inout conf_short3_uv : float2
var @out conf_short3_color : float4

[fragment_program(name="conf_short3_glsl")]
def conf_short3_fragment {
    let v = short3(1, 2, 3)
    let s = short3(int(conf_short3_uv.x))
    let r = int(v.x) + int(s.x)
    conf_short3_color = float4(float(r), 0., 0., 1.)
}

[test]
def test_conf_short3_glsl(t : T?) {
    t |> run("conformance short3: emits and validates") <| @(t : T?) {
        t |> success(has(conf_short3_glsl, "i16vec3"), "spells i16vec3")
        t |> success(has(conf_short3_glsl, "#if defined(GL_NV_gpu_shader5)"), "guarded NV chain")
        t |> success(has(conf_short3_glsl, "GL_EXT_shader_explicit_arithmetic_types_int16"), "EXT line in the #else branch")
        let r = validate_glsl(conf_short3_glsl, "frag")
        if (r.ran) {
            t |> success(r.ok, "glslang: {r.msg}")
        } else {
            feint("glslangValidator not found locally; skipping (CI enforces)\n")
        }
    }
}
