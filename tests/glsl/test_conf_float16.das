// GENERATED by utils/internal/dasgen/gen_shader_conformance.das — DO NOT EDIT
// regen: daslang utils/internal/dasgen/gen_shader_conformance.das
options gen2
options indenting = 4

require dastest/testing_boost public
require _glsl_common
require glsl/glsl_opengl

var @inout conf_float16_uv : float2
var @out conf_float16_color : float4

[fragment_program(name="conf_float16_glsl")]
def conf_float16_fragment {
    var v = 1.5h
    let s = float16(conf_float16_uv.x)
    v = v + s
    v = v * 2h
    v = v - s
    let r = v
    conf_float16_color = float4(float(r), 0., 0., 1.)
}

[test]
def test_conf_float16_glsl(t : T?) {
    t |> run("conformance float16: emits and validates") <| @(t : T?) {
        t |> success(has(conf_float16_glsl, "float16_t"), "spells float16_t")
        t |> success(has(conf_float16_glsl, "#if defined(GL_NV_gpu_shader5)"), "guarded NV chain")
        t |> success(has(conf_float16_glsl, "GL_EXT_shader_explicit_arithmetic_types_float16"), "EXT line in the #else branch")
        let r = validate_glsl(conf_float16_glsl, "frag")
        if (r.ran) {
            t |> success(r.ok, "glslang: {r.msg}")
        } else {
            feint("glslangValidator not found locally; skipping (CI enforces)\n")
        }
    }
}
