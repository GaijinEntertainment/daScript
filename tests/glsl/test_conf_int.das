// GENERATED by utils/internal/dasgen/gen_shader_conformance.das — DO NOT EDIT
// regen: daslang utils/internal/dasgen/gen_shader_conformance.das
options gen2
options indenting = 4

require dastest/testing_boost public
require _glsl_common
require glsl/glsl_opengl

var @inout conf_int_uv : float2
var @out conf_int_color : float4

[fragment_program(name="conf_int_glsl")]
def conf_int_fragment {
    var v = 1
    let s = int(conf_int_uv.x)
    v = v + s
    v = v * 2
    v = v - s
    let r = v
    conf_int_color = float4(float(r), 0., 0., 1.)
}

[test]
def test_conf_int_glsl(t : T?) {
    t |> run("conformance int: emits and validates") <| @(t : T?) {
        t |> success(has(conf_int_glsl, "int"), "spells int")
        let r = validate_glsl(conf_int_glsl, "frag")
        if (r.ran) {
            t |> success(r.ok, "glslang: {r.msg}")
        } else {
            feint("glslangValidator not found locally; skipping (CI enforces)\n")
        }
    }
}
