// GENERATED by utils/internal/dasgen/gen_shader_conformance.das — DO NOT EDIT
// regen: daslang utils/internal/dasgen/gen_shader_conformance.das
options gen2
options indenting = 4

require dastest/testing_boost public
require _glsl_common
require glsl/glsl_opengl

var @inout conf_ushort2_uv : float2
var @out conf_ushort2_color : float4

[fragment_program(name="conf_ushort2_glsl")]
def conf_ushort2_fragment {
    let v = ushort2(1u, 2u)
    let s = ushort2(uint(conf_ushort2_uv.x))
    let r = uint(v.x) + uint(s.x)
    conf_ushort2_color = float4(float(r), 0., 0., 1.)
}

[test]
def test_conf_ushort2_glsl(t : T?) {
    t |> run("conformance ushort2: emits and validates") <| @(t : T?) {
        t |> success(has(conf_ushort2_glsl, "u16vec2"), "spells u16vec2")
        t |> success(has(conf_ushort2_glsl, "#if defined(GL_NV_gpu_shader5)"), "guarded NV chain")
        t |> success(has(conf_ushort2_glsl, "GL_EXT_shader_explicit_arithmetic_types_int16"), "EXT line in the #else branch")
        let r = validate_glsl(conf_ushort2_glsl, "frag")
        if (r.ran) {
            t |> success(r.ok, "glslang: {r.msg}")
        } else {
            feint("glslangValidator not found locally; skipping (CI enforces)\n")
        }
    }
}
