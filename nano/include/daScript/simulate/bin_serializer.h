#pragma once

// nano shadow of daScript/simulate/bin_serializer.h.
//
// Generated standalone code includes this header because the `$` builtin module
// declares it, never because the emitted code names a symbol from it. The real
// header pulls the serializer and its STL containers into every generated TU;
// nano keeps the include valid and empty.
