{
    "name": "dasllama-server-e2e",
    "private": true,
    "description": "Playwright tests for the dasllama-server control page — fixtures captured from a real server, served by route interception (no model needed).",
    "scripts": {
        "test": "playwright test",
        "test:headed": "playwright test --headed"
    },
    "devDependencies": {
        "@playwright/test": "^1.50.0"
    }
}
