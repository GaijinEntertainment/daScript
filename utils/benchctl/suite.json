{
    "root": "benchmarks",
    "exclude": [
        "**/tests/**",
        "**/_*.das",
        "core/jobque/dispatch_timeline.das"
    ],
    "lanes": [
        "interp",
        "jit",
        "aot"
    ],
    "repeat": 3,
    "timeout_seconds": 900,
    "files": {
        "core/array/test01.das": {
            "skip": "allocates ~19 GB under persistent_heap and gets OOM-killed; unskip once the benchmark is fixed"
        },
        "compile/utils.das": {
            "timeout_seconds": 7200
        }
    },
    "lane_excludes": {
        "jit": {
            "compile/**": "times a child daslang -compile-only; the lane the parent runs in changes nothing about the child, and the interp lane already carries the group",
            "core/gc/test01.das": "heap_collect cannot attribute the locals of a frame entered through a compiled frame, so gc_mark_heavy measures nothing under jit",
            "core/gc/escape_free.das": "force_escape_free frees a non-escaping allocation at scope exit as a heap chunk under jit, and the child dies on 'not a chunk pointer'"
        },
        "aot": {
            "compile/**": "times a child daslang -compile-only; the lane the parent runs in changes nothing about the child, and the interp lane already carries the group",
            "core/gc/test01.das": "heap_collect cannot attribute the locals of a frame entered through a compiled frame - the AOT'd dastest runner calls run_impl - so the gc benchmarks measure nothing under aot"
        }
    }
}
