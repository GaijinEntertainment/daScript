# LAWS

Append-only intent provenance for rulings applied to the documents in this folder. Not a
rule document: entries are never cited as rules and are never edited or compacted.

## 2026-09-03 - README.md

Boris: the `(probe-verified <date>)` tags in `skills/daslang/references` are wasting tokens.
Shorten the text too. This is content that gets loaded into the context, and people use it
with weaker models.

Applied: the "Rules for editing" entry now forbids inline verification tags in these files;
the verification practice itself (probe before landing) is unchanged.
