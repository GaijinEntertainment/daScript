{
  "name": "daslang-lsp",
  "description": "daslang language server — compiler diagnostics after every edit, plus definition/hover/references/symbols",
  "lspServers": {
    "daslang": {
      "command": "${CLAUDE_PLUGIN_ROOT}/../../../bin/watchdog",
      "args": ["--lsp"],
      "extensionToLanguage": { ".das": "daslang" }
    }
  }
}
